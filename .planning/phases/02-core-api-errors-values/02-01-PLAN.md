---
phase: 02-core-api-errors-values
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ort/errors.go
  - ort/errors_test.go
  - ort/errors_native_test.go
  - ort/environment.go
  - ort/environment_test.go
autonomous: true
requirements: [API-02]

must_haves:
  truths:
    - "Callers can inspect every converted native ONNX Runtime failure as *ORTError with its operation, ErrorCode, and Go-owned message intact after status release (D-10, D-11)."
    - "A zero native status returns nil, while each nonzero native status is released exactly once even under concurrent conversion (D-11, D-23)."
    - "Callers can classify local invalid-argument, uninitialized, destroyed-resource, shared-library-not-found, and existing unsupported-platform failures with errors.Is without conflating them with native ORT codes (D-12)."
    - "Every production statusToError call executes while the caller holds ortCallMu lifecycle protection (or InitializeEnvironment's stronger write-lock scope), so registered status accessors cannot be cleared during conversion (D-11)."
    - "D-15: Useful existing operation prefixes remain where practical, while tests and callers treat errors.Is/errors.As and typed fields—not exact complete text—as the compatibility contract."
  artifacts:
    - path: "ort/errors.go"
      provides: "Public ORTError and lean sentinel contracts plus the single native-status conversion owner"
      exports: ["ORTError", "ErrInvalidArgument", "ErrNotInitialized", "ErrDestroyed", "ErrSharedLibraryNotFound"]
    - path: "ort/errors_test.go"
      provides: "Race-safe fake-status ownership, error inspection, and sentinel wrapping tests"
      contains: "TestStatusToError"
    - path: "ort/errors_native_test.go"
      provides: "Real ONNX Runtime CreateStatus/GetErrorCode/GetErrorMessage/ReleaseStatus ABI round trip"
      contains: "TestNativeORTStatusRoundTrip"
    - path: "ort/environment.go"
      provides: "GetErrorCode function-pointer registration and lifecycle reset"
      contains: "getErrorCodeFunc"
  key_links:
    - from: "ort/errors.go"
      to: "ort/environment.go"
      via: "statusToError uses the registered GetErrorCode/GetErrorMessage/ReleaseStatus functions without adding a lock to the established ORT lock graph"
      pattern: "getErrorCodeFunc|getErrorMessageFunc|releaseStatusFunc"
    - from: "ort/errors_native_test.go"
      to: "internal/c_api/onnxruntime_c_api.h"
      via: "real non-race purego ABI registration"
      pattern: "TestNativeORTStatusRoundTrip"
---

<objective>
Establish the Phase 2 error contracts and the one native-status ownership path before any resource call site migrates.

Purpose: API-02 depends on a stable, inspectable error surface and exact native lifetime handling. This plan implements D-10, D-11, D-12, and the two-layer verification required by D-23, using only the standard library and existing purego boundary.
Output: `ort/errors.go`, fake/race tests, a real non-race ABI test, and `GetErrorCode` registration/reset support.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-api-errors-values/02-CONTEXT.md
@.planning/phases/02-core-api-errors-values/02-RESEARCH.md
@.planning/phases/02-core-api-errors-values/02-PATTERNS.md
@.planning/phases/02-core-api-errors-values/02-VALIDATION.md
</context>

<interfaces>
<!-- Contracts created here and consumed by Plans 04-07. -->

Target public error surface:
```go
type ORTError struct {
	Operation string
	Code      ErrorCode
	Message   string
}

var ErrInvalidArgument error
var ErrNotInitialized error
var ErrDestroyed error
var ErrSharedLibraryNotFound error
```

Existing native API fields from `ort/ortapi_generated.go`:
```go
CreateStatus    uintptr
GetErrorCode    uintptr
GetErrorMessage uintptr
ReleaseStatus   uintptr
```

Existing Phase 1 sentinel remains defined once in `ort/bootstrap.go`:
```go
var ErrUnsupportedPlatform = errors.New("unsupported platform for ONNX Runtime bootstrap")
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>02-01-01: Define inspectable errors and prove fake-status ownership under race</name>
  <files>ort/errors.go, ort/errors_test.go</files>
  <read_first>
    - .planning/phases/02-core-api-errors-values/02-CONTEXT.md (D-10 through D-16 and D-23)
    - .planning/phases/02-core-api-errors-values/02-REVIEWS.md (sentinel semantics and status-converter lifecycle findings)
    - .planning/phases/02-core-api-errors-values/02-RESEARCH.md (Pattern 3, Pattern 4, Pitfalls 1-3 and 7)
    - .planning/phases/02-core-api-errors-values/02-PATTERNS.md (`ort/errors.go` and `ort/errors_test.go` assignments)
    - .planning/spikes/001-ort-status-lifetime/status_lifetime_test.go (validated fake store, copy-before-release, and 256-worker proof)
    - ort/errors.go (new file; confirm it does not exist before creation)
    - ort/errors_test.go (new file; confirm it does not exist before creation)
    - ort/constants.go (existing ErrorCode values)
    - ort/bootstrap.go (existing ErrUnsupportedPlatform; do not duplicate it)
    - ort/cstring.go (native string-copy helper)
  </read_first>
  <behavior>
    - Test: zero status returns nil and invokes neither an accessor nor release.
    - Test: nonzero status yields an error discoverable with `errors.As` as `*ORTError`, with exact `Operation`, `Code`, and copied `Message` fields.
    - Test: release overwrites the fake native message backing, but `ORTError.Message` remains unchanged and each status release count is exactly one.
    - Test: 256 independent conversions pass under `-race`, each retaining its own message and one release.
    - Test: if a fake accessor panics, the release defer already installed by the converter still runs exactly once.
    - Test: each public local sentinel remains discoverable through one or more `%w` wrappers with `errors.Is`; a native `*ORTError` does not automatically match a local sentinel.
  </behavior>
  <action>
    Write `ort/errors_test.go` first from the validated status-lifetime spike and confirm the new tests fail because the production contracts do not exist. Use the exact top-level test names `TestStatusToError`, `TestORTError`, and `TestErrorSentinel`, with table/subtests beneath them for the behavior matrix, so the race selector can be anchored and cannot select the native ABI test. Then create `ort/errors.go`.

    Per D-10, define a doc-commented exported `ORTError` with exported `Operation string`, `Code ErrorCode`, and `Message string` fields and an `Error() string` method whose text contains all three values. Store no status handle, native pointer, `unsafe.Pointer`, or native-backed byte slice. Do not implement `Unwrap` or translate native codes to Go sentinels: callers use `errors.As` for `ORTError` and its `Code`.

    Per D-12 and the planner discretion recorded in research, define the lean, doc-commented sentinels `ErrInvalidArgument`, `ErrNotInitialized`, `ErrDestroyed`, and `ErrSharedLibraryNotFound`. Make their boundaries explicit: `ErrInvalidArgument` covers invalid explicit arguments/options; `ErrNotInitialized` covers missing ORT runtime/function/configuration state required for an operation, including an unset library path at direct environment initialization; `ErrDestroyed` covers a once-live resource whose handle is gone; `ErrSharedLibraryNotFound` covers a supported-platform lookup that completed without finding a usable library. Keep Phase 1's `ErrUnsupportedPlatform` where it is and do not create retry-policy or operation-specific public sentinels. This addresses the review concern that uninitialized, unconfigured, and library-not-found states could otherwise become semantically interchangeable.

    Per D-11, add a private injectable `statusOps` seam and a conversion helper used by the tests. Status `0` returns `nil`. For nonzero status, install `defer ops.release(status)` before invoking either accessor, then copy the code and message into `ORTError`. The helper owns the only release. Keep the seam private and standard-library-only.

    Make the fake store mutate its message buffer during release, record accessor/release ordering, and synchronize its maps/counters. Use `errors.As`/`errors.Is` and field assertions rather than full error-string equality. Do not add or change a module dependency.
  </action>
  <verify>
    <automated>go test -race ./ort -run '^(TestStatusToError|TestORTError|TestErrorSentinel)$'</automated>
  </verify>
  <acceptance_criteria>
    - `ort/errors.go` exports exactly `ORTError`, `ErrInvalidArgument`, `ErrNotInitialized`, `ErrDestroyed`, and `ErrSharedLibraryNotFound` from the new error surface; `ErrUnsupportedPlatform` remains defined only in `ort/bootstrap.go`.
    - `ORTError` contains `Operation string`, `Code ErrorCode`, and `Message string`, and contains no native handle or pointer field.
    - `statusToErrorWithOps(0, ...)` returns nil without release; every nonzero fake status is released exactly once after its code and message are copied.
    - `go test -race ./ort -run '^(TestStatusToError|TestORTError|TestErrorSentinel)$'` exits 0.
    - `git diff --exit-code -- go.mod go.sum` exits 0.
  </acceptance_criteria>
  <done>The public error categories and typed native error exist, and fake/race tests prove copy-before-release, defer ordering, exact one-release accounting, and Is/As separation.</done>
</task>

<task type="auto" tdd="true">
  <name>02-01-02: Register native error-code access and validate the real ABI without race</name>
  <files>ort/errors.go, ort/errors_native_test.go, ort/environment.go, ort/environment_test.go</files>
  <read_first>
    - ort/errors.go and ort/errors_test.go (Task 02-01-01 contracts and ownership seam)
    - .planning/phases/02-core-api-errors-values/02-REVIEWS.md (statusToError lifecycle-precondition and platform-coverage findings)
    - ort/errors_native_test.go (new file; confirm it does not exist before creation)
    - ort/environment.go (global function pointers, clearORTGlobalsLocked, InitializeEnvironment registration, and lock hierarchy)
    - ort/environment_test.go (resetEnvironmentState and existing error-message/release helper tests)
    - ort/ortapi_generated.go (CreateStatus/GetErrorCode/GetErrorMessage/ReleaseStatus fields)
    - internal/c_api/onnxruntime_c_api.h (OrtStatus ownership comments and function signatures)
    - .planning/spikes/001-ort-status-lifetime/native_status_test.go (validated real ABI proof)
    - .planning/spikes/001-ort-status-lifetime/README.md (required race/native separation)
    - .planning/phases/02-core-api-errors-values/02-CONTEXT.md (D-11 and D-23)
  </read_first>
  <behavior>
    - Test: `getErrorCodeFunc` is registered and cleared/reset in lockstep with message/release functions.
    - Test: cross-platform registration/reset tests exercise the status function-pointer contract without requiring a native library.
    - Test: with `ONNXRUNTIME_LIB_PATH` set, a real native status created with `ErrorCodeInvalidGraph` and a known message converts to `*ORTError` with the same code/message after release.
    - Test: with `ONNXRUNTIME_LIB_PATH` unset, the native test skips with an actionable reason and the fake/race suite remains runnable.
    - Test: the native round trip runs without `-race`; no checkptr-disabling flag is introduced.
  </behavior>
  <action>
    Write `ort/errors_native_test.go` before production registration. Because the validated purego `Dlopen`/`Dlsym` analog is Unix-only, make `//go:build !windows` the first line followed by the required blank line; do not let this file compile into the supported Windows test matrix. Gate the exact top-level `TestNativeORTStatusRoundTrip` on non-empty `ONNXRUNTIME_LIB_PATH`, use that exact library path rather than a developer cache, register the real CreateStatus/GetErrorCode/GetErrorMessage/ReleaseStatus functions through the existing purego ABI, and assert the converted code/message survive release. Keep this test out of race/checkptr execution per D-23.

    In `ort/environment.go`, add `getErrorCodeFunc func(uintptr) ErrorCode` beside `getErrorMessageFunc` and `releaseStatusFunc`; register it from `ortAPI.GetErrorCode`, clear it in `clearORTGlobalsLocked`, and add it to `resetEnvironmentState` in `ort/environment_test.go`. Add the exact top-level `TestEnvironmentErrorFunctionRegistration` for this registration/clear/reset contract. Preserve the documented `AdvancedSession.runMu → ortCallMu → mu → Tensor.runMu` hierarchy.

    Complete the production `statusToError(status, operation)` adapter in `ort/errors.go` using the registered code/message/release functions and `CstringToGo`. It must reuse the Task 02-01-01 ownership helper and acquire no new lock. Addresses review concern: document the non-negotiable precondition directly on the helper—every production caller holds `ortCallMu` for the complete native call and conversion, while `InitializeEnvironment` holds `ortCallMu.Lock` plus `mu`; those scopes prevent clear/reset from racing the registered accessors. Do not add an unsafe nil-function fallback for a nonzero status because a missing `ReleaseStatus` function cannot satisfy D-11's exact-release contract; instead, keep registration checks before native calls and make Plan 08 audit every call site for the required lifecycle scope. Do not delete the existing message/release compatibility helpers yet; Plans 04-06 remove their call sites after the shared converter exists.

    Treat native-platform coverage deliberately. The real `CreateStatus` round trip is Unix-loader coverage wherever `ONNXRUNTIME_LIB_PATH` is configured; it is not a Windows DLL ABI proof. Retain cross-platform `TestEnvironmentErrorFunctionRegistration` plus the Windows package compile as the Phase 2 Windows evidence, and record the native-test platform actually exercised in the summary. Do not bake a developer cache path into the test.
  </action>
  <verify>
    <automated>go test -short ./ort -run '^(TestStatusToError|TestORTError|TestEnvironmentErrorFunctionRegistration)$' &amp;&amp; ONNXRUNTIME_LIB_PATH="${ONNXRUNTIME_LIB_PATH:-}" go test ./ort -run '^TestNativeORTStatusRoundTrip$' &amp;&amp; GOOS=windows GOARCH=amd64 go test -c -o /dev/null ./ort</automated>
  </verify>
  <acceptance_criteria>
    - `ort/errors_native_test.go` starts with `//go:build !windows` and its Unix-only purego loader code is excluded from Windows builds.
    - `getErrorCodeFunc` is declared, registered from `ortAPI.GetErrorCode`, cleared by `clearORTGlobalsLocked`, and reset by `resetEnvironmentState`.
    - `statusToError` uses the single release-owning helper and copies the native message with `CstringToGo` before the deferred release.
    - `statusToError` documents its `ortCallMu`/initialization lock precondition, and every production call site is assigned to Plans 04-06 plus the final Plan 08 lifecycle-scope audit.
    - `TestNativeORTStatusRoundTrip` skips when `ONNXRUNTIME_LIB_PATH` is empty and passes against a valid configured runtime without `-race`.
    - `GOOS=windows GOARCH=amd64 go test -c -o /dev/null ./ort` exits 0, proving the supported Windows package/test matrix compiles with the native test excluded.
    - The summary distinguishes Unix real-ABI evidence from Windows registration/reset and compile evidence; it does not claim a Windows native status round trip.
    - No command, test, or workflow disables checkptr.
    - All three automated verification commands exit 0 (a native-test skip is acceptable only when the environment variable is absent).
  </acceptance_criteria>
  <done>The production converter can capture native code/message/release through registered ORT functions, reset state is complete, and the independent real ABI proof exists without weakening race/checkptr safety.</done>
</task>

</tasks>

<threat_model>
## Assets

- Native `OrtStatus` lifetime and its status-owned message memory
- Caller-visible error categories and native error details
- Process-global ORT function-pointer state

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ONNX Runtime native ABI → Go | A nonzero native status and status-owned message cross the purego `uintptr` boundary into Go-owned error data. |
| Package internals → public caller | Local sentinels and `ORTError` fields become compatibility contracts consumed through `errors.Is`/`errors.As`. |

## STRIDE Threat Register

Threat identifiers, categories, severities, and dispositions are canonical in `02-VALIDATION.md` § Canonical Threat Register; this table scopes those definitions to Plan 02-01.

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering / Denial of Service | HIGH | `statusToError`, GetErrorCode/GetErrorMessage/ReleaseStatus | mitigate | One helper installs release before access, copies all status-owned data, requires caller-held lifecycle protection, and proves exact one-release behavior under race plus a separate real ABI test. |
| T-02-04 | Repudiation | MEDIUM | public error chain | mitigate | Separate local sentinels (`errors.Is`) from native `ORTError` (`errors.As`) and retain operation/code/message fields. |
| T-02-09 | Denial of Service | MEDIUM | Go API baseline | mitigate | Use Go 1.25-compatible `errors.As`; do not use Go 1.26-only `errors.AsType`. |
| T-02-SC | Tampering | LOW | package-manager inputs | mitigate | Install no package and require `go.mod`/`go.sum` to remain unchanged. |

## Security Verification

- `go test -race ./ort -run '^(TestStatusToError|TestORTError|TestErrorSentinel)$'`
- `ONNXRUNTIME_LIB_PATH="$ONNXRUNTIME_LIB_PATH" go test ./ort -run '^TestNativeORTStatusRoundTrip$'` without `-race`
- `GOOS=windows GOARCH=amd64 go test -c -o /dev/null ./ort`
- `git diff --exit-code -- go.mod go.sum`

No HIGH threat is accepted or unresolved.
</threat_model>

<verification>
1. Run `go test -race ./ort -run '^(TestStatusToError|TestORTError|TestErrorSentinel)$'`.
2. Run `ONNXRUNTIME_LIB_PATH="${ONNXRUNTIME_LIB_PATH:-}" go test ./ort -run '^TestNativeORTStatusRoundTrip$'` without `-race`.
3. Run `GOOS=windows GOARCH=amd64 go test -c -o /dev/null ./ort`.
4. Run `go test -short ./ort`.
5. Confirm `getErrorCodeFunc` is declared, registered, cleared, and reset exactly once at each lifecycle point.
6. Confirm `go.mod` and `go.sum` are unchanged.
</verification>

<success_criteria>
- Native failures can be inspected as `*ORTError` with a stable Go-owned operation, code, and message.
- Every nonzero fake status is released exactly once; zero status is a no-op.
- The real native ABI proof and the race ownership proof remain separate.
- Local lifecycle/validation categories are lean and inspectable without changing native-code semantics.
- No new dependency, logging abstraction, or Go 1.26-only API is introduced.
</success_criteria>

<output>
Create `.planning/phases/02-core-api-errors-values/02-01-SUMMARY.md` when done.
</output>
