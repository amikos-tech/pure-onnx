---
phase: 02-core-api-errors-values
plan: 08
type: execute
wave: 3
depends_on: [02-04, 02-05, 02-06, 02-07]
files_modified:
  - ort/finalizer_log.go
  - ort/environment.go
  - ort/environment_test.go
  - .github/workflows/ci.yml
autonomous: true
requirements: [API-02, API-03]

must_haves:
  truths:
    - "No production ORT status call site remains open-coded: all seven use the one converter, and obsolete message/release wrappers are gone (D-11, D-14)."
    - "`ort/finalizer_log.go` is absent, no direct production ort log.Printf or printf-style finalizer helper remains, and the fourteen direct notices plus three finalizer callers obey the private structured diagnostic policy without logging returned errors (D-17 through D-22)."
    - "CI runs fake status/diagnostic/session concurrency tests under race and runs real native status plus real-model RunWithValues separately without race or disabled checkptr (D-23)."
    - "Each name-anchored CI selector proves its selected top-level tests are live before executing them, so a rename cannot silently shrink race or native coverage."
    - "The complete short, targeted race, compile, changed-code/new-issues lint, and available native suites pass with unchanged typed Tensor/Run consumers and no dependency or CI-action reference change (D-01 through D-23)."
  artifacts:
    - path: "ort/environment.go"
      provides: "Only function-pointer state needed by statusToError; no obsolete open-coded status helper"
    - path: "ort/environment_test.go"
      provides: "Tests aligned to ORTError conversion rather than removed message/release wrappers"
    - path: ".github/workflows/ci.yml"
      provides: "Separate race callback/concurrency coverage and non-race native ABI/real-model coverage"
      contains: "TestNativeORTStatusRoundTrip"
  key_links:
    - from: ".github/workflows/ci.yml (test-race-ort-concurrency)"
      to: "ort/errors_test.go, ort/diagnostics_test.go, ort/session_test.go"
      via: "targeted fake/callback/concurrency regex under -race"
      pattern: "StatusToError|Diagnostic|AdvancedSessionRunWithValues"
    - from: ".github/workflows/ci.yml (real-model integration)"
      to: "ort/errors_native_test.go and ort/session_test.go"
      via: "ONNXRUNTIME_LIB_PATH-backed non-race tests"
      pattern: "NativeORTStatusRoundTrip|RunWithValuesRealModel"
---

<objective>
Converge the parallel resource migrations, delete obsolete plumbing, wire the required race/native CI split, and prove the entire Phase 2 surface remains compatible.

Purpose: Plans 04-07 intentionally preserve a few compatibility helpers while running in parallel. After they all land, this plan removes dead code, performs the global status/log call-site audit required by D-22/D-23, and makes both missing verification layers durable in CI.
Output: deleted legacy helpers, updated CI lanes, and full phase verification evidence.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-api-errors-values/02-CONTEXT.md
@.planning/phases/02-core-api-errors-values/02-RESEARCH.md
@.planning/phases/02-core-api-errors-values/02-PATTERNS.md
@.planning/phases/02-core-api-errors-values/02-VALIDATION.md
@.planning/phases/02-core-api-errors-values/02-04-SUMMARY.md
@.planning/phases/02-core-api-errors-values/02-05-SUMMARY.md
@.planning/phases/02-core-api-errors-values/02-06-SUMMARY.md
@.planning/phases/02-core-api-errors-values/02-07-SUMMARY.md
</context>

<interfaces>
Final CI split required by D-23:
```text
Race lane: fake/instrumented status ownership + diagnostics + session/value concurrency
Native lane: real CreateStatus ABI + real-model RunWithValues, without -race
```

Final source audit target:
```text
0 production wrapper declarations/calls to getErrorMessage(status)
0 production wrapper declarations/calls to releaseStatus(status)
0 production calls to log.Printf in ort/
0 production references to logFinalizerWarning
test-only native callback names remain allowed
```
</interfaces>

<tasks>

<task type="auto">
  <name>02-08-01: Delete obsolete status/finalizer helpers and complete the global call-site audit</name>
  <files>ort/finalizer_log.go, ort/environment.go, ort/environment_test.go</files>
  <read_first>
    - ort/finalizer_log.go (delete only after confirming every session/tensor/memory caller migrated)
    - .planning/phases/02-core-api-errors-values/02-REVIEWS.md (status lifecycle, Wave 2 convergence, and source-audit findings)
    - ort/environment.go (legacy getErrorMessage/releaseStatus wrapper declarations vs. still-required function-pointer globals)
    - ort/environment_test.go (tests of legacy wrappers that must be removed/replaced, while GetErrorCode reset tests remain)
    - ort/errors.go and ort/errors_test.go (sole status conversion owner)
    - ort/diagnostics.go and ort/diagnostics_test.go (sole logging infrastructure)
    - ort/session.go, ort/tensor.go, ort/memory.go, ort/bootstrap.go (all migrated call sites from Plans 04-07)
    - .planning/phases/02-core-api-errors-values/02-CONTEXT.md (D-11, D-14, D-17 through D-23)
    - .planning/phases/02-core-api-errors-values/02-PATTERNS.md (seven native status blocks, fourteen direct log sites, and three finalizer callers)
  </read_first>
  <action>
    First run two production-only source audits over `ort/`, both excluding `*_test.go`: one for the legacy wrapper declarations/calls `getErrorMessage(` and `releaseStatus(`, and one for `log.Printf`/`logFinalizerWarning`. Treat these identifier audits as a secondary structural gate; the full short/race/native behavioral suites remain the primary proof. Stop if any session/tensor/memory finalizer still calls `logFinalizerWarning`, any of the seven native status sites still calls the old wrappers, or bootstrap/environment still uses direct `log.Printf`; fix belongs in its owning prerequisite plan, not in a new parallel implementation here. Do not reject the legitimate local `getErrorMessage(status)` callback in `ort/errors_native_test.go`; D-23 requires that native test analog.

    Once all three finalizer callers use `emitFinalizerDiagnostic`, delete `ort/finalizer_log.go` rather than retaining an unused formatting facade.

    In `ort/environment.go`, remove only the obsolete helper functions `getErrorMessage(status uintptr) string` and `releaseStatus(status uintptr)`. Retain `getErrorCodeFunc`, `getErrorMessageFunc`, and `releaseStatusFunc`, because `statusToError` still needs those registered function pointers. Remove or replace the old helper-specific tests in `ort/environment_test.go`; keep reset/registration and ORTError ownership tests.

    Per D-11/D-14, require every one of the original seven native status sites to call `statusToError` and prohibit open-coded message/release. Addresses review concern: inspect each call in `environment.go`, `memory.go`, `tensor.go`, and `session.go` and require it to remain inside the caller's `ortCallMu` read/write lifecycle scope documented by Plan 01; do not permit a future unprotected global-function-pointer read. Per D-17 through D-22, require all original fourteen direct notices plus three finalizer callers to flow through `emitDiagnostic`/`emitFinalizerDiagnostic`, with no automatic emission on returned-error paths.

    Resolve the Wave 2 integration concern at convergence: confirm each prerequisite summary exists, the resource plans changed only their declared disjoint files, and every new test that mutates ORT or diagnostic globals restores state and avoids `t.Parallel`. Then run the package-wide short and targeted race suites after all four outputs are present; file-level parallelism is retained because ownership is disjoint, while this plan supplies the required integration gate.
  </action>
  <verify>
    <automated>test ! -e ort/finalizer_log.go &amp;&amp; ! rg -n 'getErrorMessage\(|releaseStatus\(' ort --glob '*.go' --glob '!**/*_test.go' &amp;&amp; ! rg -n 'log\.Printf|logFinalizerWarning' ort --glob '*.go' --glob '!**/*_test.go' &amp;&amp; go doc ./ort.Value &gt;/dev/null &amp;&amp; go doc ./ort.IsTensor &gt;/dev/null &amp;&amp; go doc ./ort.AsTensor &gt;/dev/null &amp;&amp; go doc ./ort.AdvancedSession.RunWithValues &gt;/dev/null &amp;&amp; go doc ./ort.SetDiagnosticHandler &gt;/dev/null &amp;&amp; go test -run '^$' ./... &amp;&amp; go test -short ./ort</automated>
  </verify>
  <acceptance_criteria>
    - `ort/finalizer_log.go` does not exist.
    - `getErrorMessage(status)` and `releaseStatus(status)` wrapper functions and their direct tests do not exist; the three registered function-pointer globals remain.
    - The separate production-only source audit commands return no matches for legacy status wrappers, direct `log.Printf`, or `logFinalizerWarning`; test callback declarations are excluded.
    - `rg -n 'statusToError\(' ort/{environment,memory,tensor,session}.go` shows all seven migrated call sites.
    - Each of the seven `statusToError` sites is within a caller-held `ortCallMu` lifecycle scope (or `InitializeEnvironment`'s stronger write-lock scope), matching the helper's documented precondition.
    - All four Wave 2 summaries exist, file ownership remained disjoint, and process-global tests use cleanup restoration without `t.Parallel`.
    - `go test -short ./ort` exits 0.
  </acceptance_criteria>
  <done>Dead status/finalizer compatibility plumbing is deleted, all status ownership is centralized, and all ort diagnostics use the private structured paths only.</done>
</task>

<task type="auto">
  <name>02-08-02: Wire the race/native CI split and preserve the compatibility gates</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (test-race-ort-concurrency job and existing ONNXRUNTIME_LIB_PATH real-model integration job)
    - .planning/phases/02-core-api-errors-values/02-REVIEWS.md (selector-liveness, platform-coverage, API-surface, and brittle-audit findings)
    - .planning/phases/02-core-api-errors-values/02-VALIDATION.md (every final task ID, command, and D-23 separation)
    - .planning/phases/02-core-api-errors-values/02-CONTEXT.md (D-01, D-05, D-18, D-22, D-23)
    - .planning/phases/02-core-api-errors-values/02-RESEARCH.md (Validation Architecture and Go/checkptr constraints)
    - .planning/phases/02-core-api-errors-values/02-PATTERNS.md (`.github/workflows/ci.yml` analog)
    - ort/errors_test.go, ort/errors_native_test.go, ort/diagnostics_test.go, ort/session_test.go (exact test function names to place in CI)
    - Makefile and TESTING.md (existing local/full commands; do not modify)
    - examples/inference/main.go, embeddings/minilm/embedder.go, embeddings/splade/embedder.go, embeddings/openclip/embedder.go (unchanged compatibility consumers)
  </read_first>
  <action>
    Update the existing `test-race-ort-concurrency` job to define `RACE_SELECTOR` as the anchored, exact native-free selector `^(TestStatusToError|TestORTError|TestErrorSentinel|TestDiagnostic|TestAdvancedSessionRunWithValues|TestAdvancedSessionErrorContracts|TestAdvancedSessionDiagnosticPolicy|TestValuesToHandlesDeduplicatesRepeatedLockableValue|TestValuesToHandlesReleasesPriorLeasesOnError|TestValuesToHandlesRejectsNonComparableLockable|TestAdvancedSessionRunConcurrent|TestAdvancedSessionRunConcurrentAcrossSessionsSharingTensor|TestAdvancedSessionRunAndDestroyConcurrent|TestAdvancedSessionDestroyDoesNotBlockUnrelatedRun|TestTensorDestroyWaitsForInFlightRun|TestTensorDestroyDoesNotBlockUnrelatedRun|TestTensorDestroyConcurrentCallsReleaseOnce|TestTensorStatusConversion|TestTensorDiagnosticPolicy|TestEnvironmentErrorChains|TestEnvironmentStatusConversion|TestConcurrentInitialization|TestConcurrentDestroy|TestDiagnosticRuntimeVersion|TestMemoryInfoStatusConversion|TestDiagnosticMemoryInfo|TestDiagnosticCallSites|TestReturnedErrorsDoNotEmit)$` and reuse that variable for both liveness and execution. Before `go test -race ./ort -run "$RACE_SELECTOR"`, run `go test -list "$RACE_SELECTOR" ./ort`, count only lines beginning with `Test` using `awk`, print the selected names, and fail unless the count is exactly 28. The anchors remain mandatory: `TestAdvancedSessionRunWithValuesRealModel` must not match this race lane. Keep this lane self-contained and native-library-free.

    Update the existing non-race real-model integration step, after it exports `ONNXRUNTIME_LIB_PATH`, to define `NATIVE_SELECTOR` as the anchored selector containing `TestNativeORTStatusRoundTrip`, `TestAdvancedSessionRunWithValuesRealModel`, `TestAdvancedSessionRunWithAllMiniLML6V2`, and `TestAdvancedSessionRunWithAllMiniLML6V2MemoryStability`. Run `go test -list "$NATIVE_SELECTOR" ./ort` first and fail unless exactly four top-level names match, then run `go test -v ./ort/... -run "$NATIVE_SELECTOR"` without `-race`, `-gcflags=all=-d=checkptr=0`, or any other checkptr-disabling flag. This is deliberately Unix real-ABI coverage; the existing Windows short-test matrix must still execute cross-platform registration/reset tests and the Windows compile gate from Plan 01.

    Preserve current CI platforms/jobs, action `uses:` references, and dependency versions. Do not create a new workflow or install a package. Before committing the workflow edit, inspect only added/removed `uses:` lines with `git diff HEAD --unified=0 -- .github/workflows/ci.yml`; comparing to `HEAD` must cover both staged and unstaged changes, and the assertion must fail if any action reference changed while allowing the intentional test-selector edits. Also leave the lint job's existing `continue-on-error` state unchanged.

    Addresses the selector-brittleness review concern by making `go test -list` liveness counts—not exact YAML formatting checks—the executable contract; behavioral test runs remain the primary gate. Keep task feedback below 30 seconds with local list/liveness, anchor, checkptr, and focused diff assertions in `<verify>`. Run the Phase 2 quick suite, full short suite, targeted race suite, compile-only all-package check, available native tests, `go vet -unsafeptr=false ./ort/...`, and `make precommit-lint-new` at the plan/wave/phase gates below. Native tests may skip locally when `ONNXRUNTIME_LIB_PATH` is absent, but the integration job must select and run them after setting it. Historical repository-wide lint debt cleanup, removal of the informational lint escape hatch, and the enforcing full-tree lint outcome remain exclusively Phase 5 / CLN-01 per ROADMAP.

    Add a final exported-surface check with `go doc` for `Value`, `IsTensor`, `AsTensor`, `AdvancedSession.RunWithValues`, and `SetDiagnosticHandler`, followed by `go test -run '^$' ./...`. Use the Go 1.25 baseline from `go.mod` and CI as authoritative; do not edit the stale toolchain sentence in `PROJECT.md` during this code-focused phase.
  </action>
  <verify>
    <automated>test "$(go test -list '^(TestStatusToError|TestORTError|TestErrorSentinel|TestDiagnostic|TestAdvancedSessionRunWithValues|TestAdvancedSessionErrorContracts|TestAdvancedSessionDiagnosticPolicy|TestValuesToHandlesDeduplicatesRepeatedLockableValue|TestValuesToHandlesReleasesPriorLeasesOnError|TestValuesToHandlesRejectsNonComparableLockable|TestAdvancedSessionRunConcurrent|TestAdvancedSessionRunConcurrentAcrossSessionsSharingTensor|TestAdvancedSessionRunAndDestroyConcurrent|TestAdvancedSessionDestroyDoesNotBlockUnrelatedRun|TestTensorDestroyWaitsForInFlightRun|TestTensorDestroyDoesNotBlockUnrelatedRun|TestTensorDestroyConcurrentCallsReleaseOnce|TestTensorStatusConversion|TestTensorDiagnosticPolicy|TestEnvironmentErrorChains|TestEnvironmentStatusConversion|TestConcurrentInitialization|TestConcurrentDestroy|TestDiagnosticRuntimeVersion|TestMemoryInfoStatusConversion|TestDiagnosticMemoryInfo|TestDiagnosticCallSites|TestReturnedErrorsDoNotEmit)$' ./ort | awk '/^Test/{n++} END{print n+0}')" -eq 28 &amp;&amp; test "$(go test -list '^(TestNativeORTStatusRoundTrip|TestAdvancedSessionRunWithValuesRealModel|TestAdvancedSessionRunWithAllMiniLML6V2|TestAdvancedSessionRunWithAllMiniLML6V2MemoryStability)$' ./ort | awk '/^Test/{n++} END{print n+0}')" -eq 4 &amp;&amp; rg -F -q 'RACE_SELECTOR=' .github/workflows/ci.yml &amp;&amp; rg -F -q 'go test -list "$RACE_SELECTOR" ./ort' .github/workflows/ci.yml &amp;&amp; rg -F -q 'go test -race ./ort -run "$RACE_SELECTOR"' .github/workflows/ci.yml &amp;&amp; rg -F -q 'NATIVE_SELECTOR=' .github/workflows/ci.yml &amp;&amp; rg -F -q 'go test -list "$NATIVE_SELECTOR" ./ort' .github/workflows/ci.yml &amp;&amp; rg -F -q 'go test -v ./ort/... -run "$NATIVE_SELECTOR"' .github/workflows/ci.yml &amp;&amp; test -z "$(git diff HEAD --unified=0 -- .github/workflows/ci.yml | sed -n '/^[+-][[:space:]]*uses:/p')" &amp;&amp; test -z "$(git diff HEAD --unified=0 -- .github/workflows/ci.yml | sed -n '/^[+-][[:space:]]*continue-on-error:/p')" &amp;&amp; ! rg -n -- '-gcflags=.*checkptr=0' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - The targeted race job reuses one `^...$` selector for `go test -list` and `go test -race`, requires exactly 28 live top-level tests, and cannot match `TestAdvancedSessionRunWithValuesRealModel`.
    - The existing ONNXRUNTIME_LIB_PATH integration step reuses one anchored selector for liveness and execution, requires exactly four live top-level tests including `TestNativeORTStatusRoundTrip` and `TestAdvancedSessionRunWithValuesRealModel`, and does not use `-race`.
    - The automated workflow diff assertion finds no added or removed `.github/workflows/ci.yml` `uses:` line, and the lint job's `continue-on-error` state is unchanged.
    - No workflow line disables checkptr and no package/dependency/version is added.
    - The task-level focused liveness/static verification completes within the 30-second feedback target.
    - At the plan/wave/phase gates, `go test -short ./...`, the targeted race command, `go test -run '^$' ./...`, and `make precommit-lint-new` all exit 0.
    - With `ONNXRUNTIME_LIB_PATH` configured, both new native test names execute rather than skip.
    - Existing constructor-bound Run tests, examples, and minilm/splade/openclip packages remain unchanged and compile.
    - `go doc` resolves `Value`, `IsTensor`, `AsTensor`, `AdvancedSession.RunWithValues`, and `SetDiagnosticHandler`; the all-package compile check exits 0.
  </acceptance_criteria>
  <done>CI permanently enforces the separate race/native validation layers, workflow action references remain unchanged, fast task feedback is preserved, and comprehensive Phase 2 compatibility gates run at plan/wave/phase scope without taking ownership of Phase 5 lint debt.</done>
</task>

</tasks>

<threat_model>
## Assets

- Final source-of-truth status ownership and diagnostic paths
- CI's race/checkptr safety guarantees
- Existing public API and consumer compatibility
- Module dependency integrity

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parallel plan outputs → converged ort package | Independent resource migrations must resolve to one status owner and one private diagnostic mechanism. |
| Fake callback tests → race lane | Pure Go ownership/concurrency proof must remain native-ABI-free and checkptr-safe. |
| Native ORT library → integration lane | Real `uintptr` ABI proof must run separately without weakening global safety flags. |

## STRIDE Threat Register

Threat identifiers, categories, severities, and dispositions are canonical in `02-VALIDATION.md` § Canonical Threat Register; this table scopes those definitions to Plan 02-08.

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-02-02 | Denial of Service | HIGH | full targeted race suite | mitigate | Run session/value/resource concurrency tests under race after all migrations converge. |
| T-02-03 | Tampering / Denial of Service | HIGH | status ownership and CI split | mitigate | Audit all seven sites for one converter plus caller-held lifecycle protection; run fake exact-release tests under race and real ABI test without race. |
| T-02-07 | Information Disclosure | HIGH | all diagnostic call sites | mitigate | Global source/tests enforce private emitters, redacted attrs, and no direct printf logging. |
| T-02-08 | Repudiation / Denial of Service | MEDIUM | returned-error call-site audit | mitigate | Run the full diagnostic/returned-error tests and eliminate alternate logging helper. |
| T-02-09 | Denial of Service | MEDIUM | Go baseline, live CI selectors, and unchanged consumers | mitigate | Prove selector liveness, inspect the exported API, compile all packages, and run short/vet/lint gates using baseline-compatible APIs. |
| T-02-10 | Tampering / Elevation of Privilege | HIGH | bootstrap integrity regressions | mitigate | Run the existing HTTPS/checksum/archive containment/size/lock tests plus anchored `TestBootstrapCreatedFilePermissions` through the focused and full short-suite gates after error/diagnostic migration. |
| T-02-SC | Tampering | LOW | dependencies and CI actions | mitigate | Change no module requirement or workflow action reference; verify module files and assert the CI diff contains no added/removed `uses:` line. |

## Security Verification

- Global `rg` audit for alternate status/logging paths
- Full short and targeted race suites
- Focused bootstrap HTTPS/checksum/archive containment/size/lock regressions plus `go test -short ./ort -run '^TestBootstrapCreatedFilePermissions$'`
- Non-race native ABI/real-model integration selection
- Vet plus changed-code/new-issues lint (`make precommit-lint-new`); repository-wide lint enforcement remains Phase 5
- Unchanged module files and no added/removed `.github/workflows/ci.yml` `uses:` lines

No HIGH threat is accepted or unresolved.
</threat_model>

<verification>
1. Run both task-level automated commands.
2. Run the fast Phase 2 selector: `go test -short ./ort -run 'Test(ORTError|StatusToError|Value|AdvancedSessionRunWithValues|Diagnostic)'`.
3. Run the full short suite: `go test -short ./...`.
4. Run the targeted race selector from Task 02-08-02 exactly as written in `.github/workflows/ci.yml`.
5. Run the compile-only compatibility check: `go test -run '^$' ./...`.
6. Run the exact 28-test race-selector and 4-test native-selector liveness assertions from Task 02-08-02.
7. Run `go doc` for `Value`, `IsTensor`, `AsTensor`, `AdvancedSession.RunWithValues`, and `SetDiagnosticHandler`.
8. Run the focused permission regression: `go test -short ./ort -run '^TestBootstrapCreatedFilePermissions$'` (Unix assertions; documented Windows skip).
9. Run `ONNXRUNTIME_LIB_PATH="${ONNXRUNTIME_LIB_PATH:-}" go test ./ort -run '^(TestNativeORTStatusRoundTrip|TestAdvancedSessionRunWithValuesRealModel|TestAdvancedSessionRunWithAllMiniLML6V2|TestAdvancedSessionRunWithAllMiniLML6V2MemoryStability)$'`.
10. Run `go vet -unsafeptr=false ./ort/...`.
11. Run `make precommit-lint-new`; do not require repository-wide historical lint cleanup or change lint `continue-on-error` in Phase 2.
12. Run `git diff --check`, `git diff --exit-code -- go.mod go.sum`, and the Task 02-08-02 added/removed-`uses:` assertion before committing its workflow edit.
</verification>

<success_criteria>
- All seven native status paths use one converter and no obsolete wrapper remains.
- All approved diagnostics use the private slog path; no direct ort printf logger or returned-error duplicate remains.
- Race and native ABI proofs are separate and selected by CI.
- Race/native selectors fail if any required top-level test is renamed or absent.
- The final exported API surface resolves through `go doc`, and the Go 1.25 all-package compile gate remains green.
- Existing bootstrap integrity protections remain green through the focused T-02-10 regressions and final full-suite gate.
- Existing unit/integration tests and unchanged typed Tensor/Run consumers pass/compile.
- Vet, changed-code/new-issues lint, module-integrity, and unchanged CI-action-reference checks pass; the full-tree lint gate remains Phase 5.
- Every Phase 2 HIGH threat is mitigated with automated verification.
</success_criteria>

<output>
Create `.planning/phases/02-core-api-errors-values/02-08-SUMMARY.md` when done.
</output>
