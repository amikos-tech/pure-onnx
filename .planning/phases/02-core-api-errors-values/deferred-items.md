# Deferred Items

None.

## Resolved in Plan 02-08

- The native status test's intentional C API table conversion now has a narrow `govet` suppression.
- The diagnostic emitter's intentional nil-context fallback test now has a narrow `staticcheck` suppression.
