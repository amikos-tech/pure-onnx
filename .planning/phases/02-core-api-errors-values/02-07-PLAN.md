---
phase: 02-core-api-errors-values
plan: 07
type: execute
wave: 2
depends_on: [02-01, 02-03]
files_modified:
  - ort/bootstrap.go
  - ort/bootstrap_test.go
autonomous: true
requirements: [API-02]

must_haves:
  truths:
    - "Bootstrap validation and platform/library categories are inspectable with errors.Is, including existing ErrUnsupportedPlatform and public ErrSharedLibraryNotFound (D-12)."
    - "Filesystem, network, checksum, archive, dynamic-library, and independent cleanup causes remain reachable through %w/errors.Join with actionable operation/path context (D-13 through D-16)."
    - "All twelve current bootstrap log.Printf notices migrate to private Info/Warn structured diagnostics with redacted URLs and no credential/environment contents (D-17 through D-21)."
    - "Fallback/cleanup/lock-wait notices that cannot be returned may emit once, while every bootstrap error returned to its caller emits zero diagnostic records (D-18, D-22)."
    - "On Unix, bootstrap-created directories, installed TGZ/ZIP library files, and lock files retain least-privilege mode masks; the same regression compiles and skips POSIX-mode assertions on Windows, and this hardening is isolated from the error/diagnostic migration (T-02-10)."
  artifacts:
    - path: "ort/bootstrap.go"
      provides: "Public library-not-found classification, comprehensive wrapped causes, and structured non-returnable bootstrap diagnostics"
      contains: "ErrSharedLibraryNotFound"
    - path: "ort/bootstrap_test.go"
      provides: "Bootstrap sentinel/cause traversal, Unix-safe permission, diagnostic call-site, URL redaction, level/attr, and returned-error silence tests"
      contains: "TestBootstrapCreatedFilePermissions"
  key_links:
    - from: "ort/bootstrap.go"
      to: "ort/errors.go"
      via: "ErrInvalidArgument/ErrSharedLibraryNotFound and existing ErrUnsupportedPlatform wrapped with %w"
      pattern: "ErrSharedLibraryNotFound|ErrUnsupportedPlatform"
    - from: "ort/bootstrap.go"
      to: "ort/diagnostics.go"
      via: "approved fallback, cleanup, archive-skip, and lock-wait call sites only"
      pattern: "emitDiagnostic"
    - from: "ort/bootstrap_test.go"
      to: "net/url.URL.Redacted"
      via: "recording handler asserts credential-safe URL attrs"
      pattern: "Redacted"
---

<objective>
Complete the bootstrap slice of API-02: machine-readable categories, preserved lower causes, and an audited migration from direct logging to opt-in structured diagnostics.

Purpose: Bootstrap crosses filesystem, network, archive, checksum, lock, and platform boundaries. This plan extends its established `%w`/`errors.Join` and security patterns rather than replacing them, isolates the permission regression/hardening as its own atomic task, and converts only its twelve non-returnable notices to the standard handler contract.
Output: inspectable bootstrap errors, an independently reviewable permission-hardening commit, migrated diagnostics, and focused cause/security/call-site tests.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-api-errors-values/02-CONTEXT.md
@.planning/phases/02-core-api-errors-values/02-RESEARCH.md
@.planning/phases/02-core-api-errors-values/02-PATTERNS.md
@.planning/phases/02-core-api-errors-values/02-VALIDATION.md
@.planning/phases/02-core-api-errors-values/02-01-SUMMARY.md
@.planning/phases/02-core-api-errors-values/02-03-SUMMARY.md
</context>

<interfaces>
Existing Phase 1 category:
```go
var ErrUnsupportedPlatform error
func IsUnsupportedPlatformError(err error) bool
```

Plan 01 categories consumed here:
```go
var ErrInvalidArgument error
var ErrSharedLibraryNotFound error
```

Diagnostic contract:
```go
func emitDiagnostic(ctx context.Context, level slog.Level, message string, attrs ...slog.Attr)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>02-07-01: Make bootstrap validation/library categories and lower causes inspectable</name>
  <files>ort/bootstrap.go, ort/bootstrap_test.go</files>
  <read_first>
    - .planning/phases/02-core-api-errors-values/02-CONTEXT.md (D-12 through D-16)
    - .planning/phases/02-core-api-errors-values/02-REVIEWS.md (bootstrap-scope isolation and Wave 2 integration findings)
    - .planning/phases/02-core-api-errors-values/02-RESEARCH.md (Pattern 4, error-chain guidance, and bootstrap security domain)
    - .planning/phases/02-core-api-errors-values/02-PATTERNS.md (`ort/bootstrap.go` preserved-cause/sentinel assignments and bootstrap tests)
    - ort/bootstrap.go (sentinels, options/config validation, artifact resolution, download/cache/archive/checksum/lock cleanup, and errors.Join sites)
    - ort/bootstrap_test.go (ErrUnsupportedPlatform, option validation, HTTP/cache/archive/security, and cleanup tests)
    - ort/errors.go and ort/errors_test.go (ErrInvalidArgument and ErrSharedLibraryNotFound contracts)
    - .planning/phases/01-dx-test-hardening/01-CONTEXT.md (D-01/D-02 sentinel and message-compatibility precedent)
    - .planning/phases/01-dx-test-hardening/01-01-SUMMARY.md (shipped ErrUnsupportedPlatform behavior)
  </read_first>
  <behavior>
    - Test: unsupported GOOS/GOARCH remains discoverable as `ErrUnsupportedPlatform`.
    - Test: supported-platform library absence is discoverable as public `ErrSharedLibraryNotFound`, distinct from unsupported platform.
    - Test: empty/invalid bootstrap options and environment configuration match `ErrInvalidArgument` without losing the option/env identifier.
    - Test: representative filesystem, network, checksum, archive, and dynamic-library causes remain reachable through wrapping.
    - Test: a primary bootstrap failure plus independent close/remove cleanup failure preserves both through `errors.Join`.
    - Test: internal retry/permanence sentinels stay private and exact complete error text is not asserted.
  </behavior>
  <action>
    Extend tests first with `errors.Is`/`errors.As` assertions and synthetic underlying causes. Put the representative lower-cause and primary-plus-cleanup matrix under the exact top-level name `TestBootstrapErrorChains`; extend `TestResolveRuntimeArtifact` and the existing exact option/config test names for category checks. Retain only required actionable text fragments such as the affected path/option; do not compare complete messages.

    Per D-12, replace the private `errSharedLibraryNotFound` category with the public `ErrSharedLibraryNotFound` supplied by Plan 01 and update every internal `errors.Is`/wrapping site. Keep `ErrUnsupportedPlatform` and `IsUnsupportedPlatformError` source-compatible and semantically separate. Keep retry, redirect-policy, and permanence control sentinels private.

    Wrap public bootstrap option/config validation failures with `ErrInvalidArgument`, naming the failing option or environment key. Do not classify a native/OS/network failure as a local validation sentinel.

    Per D-13, audit every returned bootstrap error and preserve the current underlying filesystem/network/checksum/archive/dynamic-loader cause with `%w`; preserve independent primary+cleanup failures with `errors.Join`. Per D-15/D-16, retain operation, relevant path/artifact/identifier, and useful detail, but add no stack, source file, or line number and do not promise exact text.

    Preserve all other existing bootstrap integrity behavior: HTTPS downgrade rejection, URL redaction, archive path containment, size limits, checksum verification, and lock semantics. This task changes error contracts only; Task 02-07-02 owns the permission regression/hardening, and neither task changes bootstrap selection or download behavior.

    Addresses the divergent Wave 2 review finding by keeping this plan's ownership exclusive to `ort/bootstrap.go` and `ort/bootstrap_test.go`; do not edit shared contracts or sibling resource files. Error tests that mutate bootstrap globals must restore them with `t.Cleanup` and must not use `t.Parallel`.
  </action>
  <verify>
    <automated>go test -short ./ort -run '^(TestErrorSentinel|TestBootstrapErrorChains|TestResolveRuntimeArtifact|TestWithBootstrapVersionRejectsEmpty|TestWithBootstrapLibraryPathAndCacheDirRejectEmpty|TestWithBootstrapExpectedSHA256Validation|TestWithBootstrapBaseURLValidation|TestResolveBootstrapConfigRejectsInvalidDisableDownloadEnv|TestEnsureOnnxRuntimeSharedLibraryChecksumMismatch|TestEnsureOnnxRuntimeSharedLibraryInvalidArchive|TestRejectHTTPSDowngradeRedirect|TestDownloadRuntimeArchiveRejectsOversize|TestSecureArchiveJoin)$'</automated>
  </verify>
  <acceptance_criteria>
    - `errors.Is` distinguishes `ErrUnsupportedPlatform`, `ErrSharedLibraryNotFound`, and `ErrInvalidArgument` in their respective paths.
    - `errSharedLibraryNotFound` no longer exists; retry/permanence sentinels remain unexported.
    - Representative filesystem/network/archive/checksum causes and primary+cleanup errors remain traversable.
    - Existing bootstrap security/integrity tests continue to pass.
    - New error tests that mutate bootstrap state restore it with cleanup and do not call `t.Parallel`.
    - The targeted automated command exits 0 and module files remain unchanged.
  </acceptance_criteria>
  <done>Bootstrap exposes only actionable public categories, retains every useful lower/cleanup cause, and preserves existing download/cache/archive integrity behavior without mixing the separate permission-hardening diff into this task.</done>
</task>

<task type="auto" tdd="true">
  <name>02-07-02: Isolate bootstrap-created file permission hardening</name>
  <files>ort/bootstrap.go, ort/bootstrap_test.go</files>
  <read_first>
    - ort/bootstrap.go and ort/bootstrap_test.go (Task 02-07-01 error-contract state plus cache, staging, TGZ, ZIP, and file-lock creation paths)
    - .planning/phases/02-core-api-errors-values/02-REVIEWS.md (consensus requirement to isolate permission hardening from the error migration)
    - .planning/phases/02-core-api-errors-values/02-VALIDATION.md (canonical T-02-10 scope and automated evidence)
    - .planning/phases/02-core-api-errors-values/02-RESEARCH.md (bootstrap security controls and Phase 2 scope)
    - .planning/phases/02-core-api-errors-values/02-PATTERNS.md (`TestBootstrapCreatedFilePermissions` Unix/Windows pattern)
    - ort/bootstrap_lock_unix.go and ort/bootstrap_lock_windows.go (platform-specific lock implementation and mode semantics)
  </read_first>
  <behavior>
    - Test: on Unix, bootstrap-created cache/install/lock directories grant owner access while granting neither group write nor any access to other users.
    - Test: installed library files extracted from permissive TGZ and ZIP entries retain required owner read/execute bits and strip group/other write bits.
    - Test: lock files retain owner read/write and no group/other permissions.
    - Test: the exact test compiles on Windows and skips only POSIX mode assertions because Windows ACLs do not expose portable Unix mode semantics.
    - Regression: HTTPS, checksum, archive containment, size, and lock-integrity tests remain green.
  </behavior>
  <action>
    Addresses review concern: keep the security change independently reviewable by implementing it in this dedicated task and commit, after Task 02-07-01's error migration and before diagnostic migration. Do not mix sentinel, wrapping, or slog call-site edits into this task.

    Write the exact top-level `TestBootstrapCreatedFilePermissions` first in `ort/bootstrap_test.go`. Keep it in the cross-platform file and call `t.Skip` immediately on `runtime.GOOS == "windows"` with a POSIX-mode reason; the rest of the bootstrap suite must remain runnable there. On Unix, use fresh cache/install/lock paths plus synthetic TGZ and ZIP library entries so production helpers—not test-only chmod calls—create every inspected object.

    Assert cache/install/lock directories retain owner access with no group-write or other-user bits, installed libraries retain required owner read/execute with no group/other write bits, and lock files retain owner read/write with no group/other permissions. Feed permissive archive modes to prove unsafe bits are removed. If the red test exposes archive-derived write bits, clamp only group/other write permissions in both tar and ZIP regular-file creation while retaining executable bits; do not recursively chmod, alter unrelated paths, or broaden an existing mode.

    Preserve all existing bootstrap HTTPS downgrade, checksum, archive containment, extracted/download size, redaction, and lock-integrity behavior. Tests that mutate package globals must restore them with `t.Cleanup` and must not use `t.Parallel`.
  </action>
  <verify>
    <automated>go test -short ./ort -run '^(TestBootstrapCreatedFilePermissions|TestEnsureOnnxRuntimeSharedLibraryChecksumMismatch|TestRejectHTTPSDowngradeRedirect|TestDownloadRuntimeArchiveRejectsOversize|TestSecureArchiveJoin|TestWithProcessFileLockTimesOut)$'</automated>
  </verify>
  <acceptance_criteria>
    - `TestBootstrapCreatedFilePermissions` exercises production directory, TGZ, ZIP, and lock-file creation paths.
    - Unix assertions prove the exact least-privilege masks; the same test compiles on Windows and reports only the documented POSIX-mode skip.
    - Any production fix changes only the tar/ZIP regular-file mode mask required by the failing assertion and preserves owner execute bits.
    - Existing HTTPS/checksum/containment/size/lock regressions in the automated selector exit 0.
    - The task produces a commit separate from the error-contract and diagnostic commits, and module files remain unchanged.
  </acceptance_criteria>
  <done>Bootstrap permission coverage and any required mode clamping are isolated in one security-focused commit with Unix proof and Windows-safe compilation.</done>
</task>

<task type="auto" tdd="true">
  <name>02-07-03: Replace all twelve bootstrap log sites with audited structured diagnostics</name>
  <files>ort/bootstrap.go, ort/bootstrap_test.go</files>
  <read_first>
    - ort/bootstrap.go and ort/bootstrap_test.go (Tasks 02-07-01/02 error and permission state plus all current log.Printf call sites)
    - .planning/phases/02-core-api-errors-values/02-REVIEWS.md (handler-panic policy, diagnostic-state isolation, and bootstrap-scope findings)
    - ort/diagnostics.go and ort/diagnostics_test.go (private emitter, levels, attrs, nil reset, and recording handlers)
    - .planning/phases/02-core-api-errors-values/02-CONTEXT.md (D-17 through D-22)
    - .planning/phases/02-core-api-errors-values/02-RESEARCH.md (diagnostic call-site count, allowed attrs, Pitfalls 8-9)
    - .planning/phases/02-core-api-errors-values/02-PATTERNS.md (bootstrap diagnostic migration audit table and test-state cleanup)
  </read_first>
  <behavior>
    - Test: nil/default handler produces no output at every approved bootstrap notice.
    - Test: temporary archive removal, staging cleanup, metadata-checksum fallback, archive-glob failure, unsafe/unsupported entry skip, and user-cache fallback emit Warn with selected structured attrs.
    - Test: bootstrap lock waiting emits Info with `path` and `wait_duration`.
    - Test: download-without-checksum warning carries only a redacted URL and safe checksum state; credentials/tokens/raw environment values never appear.
    - Test: each approved notice emits once when its path is exercised.
    - Test: a deliberately panicking consumer handler invoked by a representative non-finalizer bootstrap notice propagates to the synchronous caller, proving all twelve sites retain the trusted-callback policy rather than adding local recovery.
    - Test: bootstrap failures returned to callers, including validation/network/checksum/archive/lock errors, emit zero records.
  </behavior>
  <action>
    Add recording-handler call-site tests first under the exact top-level names `TestDiagnosticCallSites` and `TestReturnedErrorsDoNotEmit`, restoring the handler with `t.Cleanup(func() { SetDiagnosticHandler(nil) })` and restoring every mutated bootstrap timing/global exactly as existing tests do. Include a `TestDiagnosticCallSites` subtest that installs a panicking handler, exercises one representative synchronous bootstrap notice, and asserts the panic propagates; the source audit then proves all twelve sites use the same unrecovered general emitter. These process-global tests must not use `t.Parallel`.

    Per D-18/D-21, replace all twelve direct `log.Printf` sites in `ort/bootstrap.go`, and no returned-error site, with the private emitter:
    1. temporary archive removal failure — Warn with `operation`, `path`, `error`;
    2. download continued without checksum — Warn with redacted `url` and safe checksum state;
    3. staging cleanup failure — Warn with `operation`, `path`, `error`;
    4. metadata lookup failure followed by pinned-checksum fallback — Warn with `operation`, `error`;
    5. tar library-glob match failure — Warn with `archive_entry`, `library_glob`, `error`;
    6. tar link entry skipped — Warn with `archive_entry`, `entry_type`;
    7. tar unsupported entry skipped — Warn with `archive_entry`, `entry_type`;
    8. zip library-glob match failure — Warn with `archive_entry`, `library_glob`, `error`;
    9. zip symlink entry skipped — Warn with `archive_entry`, `entry_type`;
    10. bootstrap lock still waiting — Info with `path`, `wait_duration`;
    11. user-cache lookup failure followed by temp-cache fallback — Warn with `path`, `error`;
    12. empty user-cache path followed by temp-cache fallback — Warn with `path`.

    Per D-19, emit only through `Logger.LogAttrs` indirectly via `emitDiagnostic`; use `slog.LevelInfo`, `slog.LevelWarn`, and standard `slog.Attr` constructors. Pass URLs through `URL.Redacted()` or an already-redacted string. Never include auth headers, tokens, raw credential-bearing URLs, or environment values. Per the Plan 03 review resolution, `SetDiagnosticHandler` establishes an explicit trusted synchronous callback boundary: do not add recovery around these non-finalizer calls, and document/test consumer-handler panic propagation under accepted T-02-06. Atomic handler state remains mitigated under T-02-11.

    Per D-22, audit every new emitter invocation: none may immediately duplicate an error returned to the caller. Add explicit zero-record tests for returned failures rather than relying on handler type. Remove the now-unused `log` import from bootstrap.go.
  </action>
  <verify>
    <automated>go test -race ./ort -run '^(TestDiagnosticCallSites|TestReturnedErrorsDoNotEmit|TestWithProcessFileLockTimesOut|TestExtractArchiveFileCrossFormat|TestExtractTGZArchiveSkipsSymlinkEntries|TestExtractZIPArchiveSkipsSymlinkEntries|TestEnsureOnnxRuntimeSharedLibraryChecksumMismatch|TestResolveRuntimeArchiveChecksumFallsBackToPinnedChecksumWhenMetadataFails)$' &amp;&amp; ! rg -n 'log\.Printf' ort/bootstrap.go</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'log\.Printf' ort/bootstrap.go` returns no matches.
    - Exactly the twelve enumerated non-returnable behaviors use `emitDiagnostic`; returned-error paths use only return/wrapping.
    - Lock waits emit Info; the other enumerated notices emit Warn with the specified attrs.
    - URL tests prove credentials are absent and the recorded URL equals its redacted form.
    - Recording-handler tests prove returned bootstrap errors emit zero records.
    - A `TestDiagnosticCallSites` subtest proves a panicking consumer handler propagates from a representative non-finalizer bootstrap notice; no bootstrap call site adds recovery.
    - Diagnostic tests restore handler/timing/global state through cleanup and do not call `t.Parallel`.
    - The targeted race command exits 0 and no logging dependency is added.
  </acceptance_criteria>
  <done>Every direct bootstrap log site is replaced by an audited Info/Warn structured notice, sensitive values are excluded, and returned errors are never duplicated.</done>
</task>

</tasks>

<threat_model>
## Assets

- Downloaded ONNX Runtime artifacts, checksums, and cache contents
- Filesystem/archive path containment and safe permissions
- Network/filesystem error causes used for remediation
- Diagnostic records that may include paths or URLs

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network/cache/archive input → local runtime artifact | Untrusted metadata, bytes, archive names, and redirects are validated by existing bootstrap controls. |
| OS/network/filesystem failure → public error | Lower causes must remain traversable without leaking into an unrelated category. |
| Bootstrap notice → consumer handler | Only selected non-returnable context may be emitted; URLs must be redacted. |

## STRIDE Threat Register

Threat identifiers, categories, severities, and dispositions are canonical in `02-VALIDATION.md` § Canonical Threat Register; this table scopes those definitions to Plan 02-07.

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-02-04 | Repudiation | MEDIUM | bootstrap error chains | mitigate | Preserve `%w`/errors.Join and assert lower/cleanup causes with Is/As. |
| T-02-06 | Denial of Service | MEDIUM | consumer handler panic at twelve bootstrap diagnostics | accept | The consumer explicitly installs trusted synchronous callback code; retain and test normal slog panic propagation rather than silently recovering at individual call sites. |
| T-02-07 | Information Disclosure | HIGH | bootstrap diagnostic attrs | mitigate | Redact URLs, allowlist attrs, exclude tokens/headers/environment values, and test recorded output. |
| T-02-08 | Repudiation / Denial of Service | MEDIUM | returned bootstrap failures | mitigate | Emit only twelve enumerated non-returnable notices and assert zero records for returned errors. |
| T-02-10 | Tampering / Elevation of Privilege | HIGH | artifact download/cache/archive handling | mitigate | Preserve HTTPS, checksum, path-containment, size, and lock tests; isolate anchored `TestBootstrapCreatedFilePermissions` plus any bounded mode clamp in Task 02-07-02 with a Windows POSIX-mode skip. |
| T-02-11 | Denial of Service | HIGH | concurrent diagnostic reconfiguration | mitigate | Reuse the atomically replaced logger state proven under race and avoid any bootstrap-local mutable handler state. |
| T-02-SC | Tampering | LOW | package-manager inputs | mitigate | Use standard errors/slog only; leave module files unchanged. |

## Security Verification

- Existing bootstrap checksum/redirect/path-traversal/size/permission tests
- `go test -short ./ort -run '^TestBootstrapCreatedFilePermissions$'` (asserts Unix modes; compiles and skips the POSIX assertion on Windows)
- Sentinel and lower-cause Is/As tests
- URL-redaction and returned-error zero-emission tests
- Source audit of all twelve diagnostic call sites

T-02-06 is accepted at MEDIUM severity only at the explicit trusted consumer-callback boundary. No HIGH threat is accepted or unresolved.
</threat_model>

<verification>
1. Run all three task-level automated commands.
2. Run `go test -short ./ort -run '^(TestBootstrapCreatedFilePermissions|Test.*Bootstrap|TestResolveRuntimeArtifact|TestWithBootstrap)'`.
3. Confirm bootstrap.go contains no `log.Printf` and exactly the approved private diagnostic invocations.
4. Confirm existing integrity/security tests still pass.
5. Confirm `go.mod` and `go.sum` are unchanged.
</verification>

<success_criteria>
- Bootstrap validation/platform/library conditions are inspectable with public sentinels.
- OS, network, filesystem, archive, checksum, and cleanup causes remain reachable.
- Existing bootstrap integrity controls do not regress, and Unix-safe directory, installed-library, and lock-file permissions are covered by an anchored automated regression with a Windows-safe skip.
- All twelve direct log sites become opt-in structured notices with safe attrs/levels.
- Returned errors emit no diagnostic records and no third-party logging/error package is added.
</success_criteria>

<output>
Create `.planning/phases/02-core-api-errors-values/02-07-SUMMARY.md` when done.
</output>
