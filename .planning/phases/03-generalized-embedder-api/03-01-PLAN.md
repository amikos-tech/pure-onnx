---
phase: 03-generalized-embedder-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - embeddings/embedder.go
  - embeddings/openclip/generalized_embedder.go
  - embeddings/embedder_test.go
autonomous: false
requirements: [API-01]

must_haves:
  truths:
    - "Go callers can use one compile-time `embeddings.Embedder[T any]` call shape for dense MiniLM/OpenCLIP rows and sparse SPLADE rows without a runtime registry, tag, union, coercion, or copy (D-01 through D-04)."
    - "MiniLM and SPLADE satisfy the shared interface through their unchanged method sets, concrete constructors, package-owned result types, inference paths, and cleanup behavior (D-05, D-09 through D-11, D-13)."
    - "OpenCLIP satisfies `embeddings.Embedder[[]float32]` through two direct text forwarders while all existing text and image methods remain unchanged and available on its concrete type (D-06, D-07, D-09, D-11, D-15)."
    - "The root production `embeddings` package contains only the dependency-free generic contract: it has zero imports and exposes no factory, implementation adapter, result union, alias, or model-specific capability package (D-02, D-03, D-08, D-10, D-12, D-14)."
    - "Compile-time assertions pin all three conformance relationships plus every existing constructor/method signature, and generic dispatch reaches the concrete validation paths (D-13, D-16)."
    - "The complete short module suite, embedding vet/regression suites, and every named native MiniLM/SPLADE/OpenCLIP integration or parity test pass; a skipped named test is a failed phase gate (D-11, D-16)."
  artifacts:
    - path: "embeddings/embedder.go"
      provides: "Public dependency-free `Embedder[T any]` contract"
      contains: "type Embedder[T any] interface"
    - path: "embeddings/openclip/generalized_embedder.go"
      provides: "Additive OpenCLIP document/query names that delegate to the existing text API"
      exports: ["EmbedDocuments", "EmbedQuery"]
    - path: "embeddings/embedder_test.go"
      provides: "External-package exact-signature, generic-conformance, typed-dispatch, and forwarding proof"
      contains: "package embeddings_test"
  key_links:
    - from: "embeddings/embedder_test.go"
      to: "embeddings/embedder.go"
      via: "compile-time assignments for dense and sparse interface instantiations"
      pattern: "embeddings\\.Embedder\\[(\\[\\]float32|splade\\.SparseVector)\\]"
    - from: "embeddings/openclip/generalized_embedder.go"
      to: "embeddings/openclip/embedder.go"
      via: "single direct returns to EmbedTexts and EmbedText"
      pattern: "return e\\.Embed(Texts|Text)\\("
    - from: "embeddings/embedder_test.go"
      to: "embeddings/minilm, embeddings/splade, embeddings/openclip"
      via: "exact constructor/method pins and typed interface dispatch"
      pattern: "NewEmbedder|EmbedDocuments|EmbedQuery|EmbedTexts|EmbedText|EmbedImages|EmbedImage|Close"
---

<objective>
Promote the already-validated generalized embedder contract into the production package graph and prove it preserves every concrete embedding API and result path.

Purpose: API-01 needs one statically typed text-embedding surface for dense and sparse rows, including SPLADE, without disturbing the settled Phase 2 core or adding runtime abstraction machinery.
Output: a zero-import root generic interface, two direct OpenCLIP forwarding methods, and compile/native regression evidence for MiniLM, SPLADE, and OpenCLIP.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-generalized-embedder-api/03-CONTEXT.md
@.planning/phases/03-generalized-embedder-api/03-RESEARCH.md
@.planning/phases/03-generalized-embedder-api/03-PATTERNS.md
@.planning/phases/03-generalized-embedder-api/03-VALIDATION.md
@.planning/phases/02-core-api-errors-values/02-CONTEXT.md
@.planning/phases/02-core-api-errors-values/02-02-SUMMARY.md
@.planning/phases/02-core-api-errors-values/02-04-SUMMARY.md
@.planning/spikes/003-generic-embedder-contract-proof/README.md
@.planning/spikes/003-generic-embedder-contract-proof/overlay/embeddings/embedder.go
@.planning/spikes/003-generic-embedder-contract-proof/overlay/embeddings/openclip/generalized_embedder.go
@.planning/spikes/003-generic-embedder-contract-proof/contract_test.go
@embeddings/minilm/embedder.go
@embeddings/splade/embedder.go
@embeddings/openclip/embedder.go
</context>

<interfaces>
<!-- These are the exact contracts validated by Spike 003. Use them directly; do not redesign them. -->

From `.planning/spikes/003-generic-embedder-contract-proof/overlay/embeddings/embedder.go`:

```go
type Embedder[T any] interface {
	EmbedDocuments(documents []string) ([]T, error)
	EmbedQuery(query string) (T, error)
	Close() error
}
```

Existing concrete signatures that remain unchanged:

```go
func (*minilm.Embedder).EmbedDocuments([]string) ([][]float32, error)
func (*minilm.Embedder).EmbedQuery(string) ([]float32, error)
func (*minilm.Embedder).Close() error

func (*splade.Embedder).EmbedDocuments([]string) ([]splade.SparseVector, error)
func (*splade.Embedder).EmbedQuery(string) (splade.SparseVector, error)
func (*splade.Embedder).Close() error

func (*openclip.Embedder).EmbedTexts([]string) ([][]float32, error)
func (*openclip.Embedder).EmbedText(string) ([]float32, error)
func (*openclip.Embedder).EmbedImages([]image.Image) ([][]float32, error)
func (*openclip.Embedder).EmbedImage(image.Image) ([]float32, error)
func (*openclip.Embedder).Close() error
```

New OpenCLIP signatures, implemented only as direct forwards:

```go
func (*openclip.Embedder).EmbedDocuments([]string) ([][]float32, error)
func (*openclip.Embedder).EmbedQuery(string) ([]float32, error)
```
</interfaces>

<dependency_graph>
- 03-01-01 needs the validated Spike 003 production blueprints and creates the root contract plus OpenCLIP's complete method set; no checkpoint.
- 03-01-02 needs 03-01-01 so all three compile-time conformance assignments can compile, and creates the permanent compatibility proof; no checkpoint.
- 03-01-03 needs 03-01-02 so the focused contract test exists, runs one fast local sample, then blocks until a configured native environment is available for the separate final phase gate; human-action checkpoint and no file edits.
</dependency_graph>

<source_coverage>
## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan task | Status |
|--------|----|----------------------|-----------|--------|
| GOAL | — | One API serves dense and sparse embeddings, including SPLADE, over the settled core | 03-01-01 through 03-01-03 | COVERED |
| REQ | API-01 | Generalized embedder API supports dense and sparse embeddings, including SPLADE | 03-01-01 through 03-01-03 | COVERED |
| RESEARCH | R-01 | Promote exactly the three Spike-proven files; add no dependency or feasibility spike | 03-01-01, 03-01-02 | COVERED |
| RESEARCH | R-02 | Keep production root import-free and cross-package assertions in `package embeddings_test` | 03-01-01, 03-01-02 | COVERED |
| RESEARCH | R-03 | Reuse current MiniLM/SPLADE/OpenCLIP regression and golden suites | 03-01-03 + final phase verification | COVERED |
| RESEARCH | R-04 | Native acceptance must prove named tests passed rather than skipped | 03-01-03 + final phase verification | COVERED |
| RESEARCH | R-05 | Preserve the Go 1.25 baseline and add no module dependency | 03-01-01, 03-01-03 | COVERED |
| CONTEXT | D-01 | Generic `embeddings.Embedder[T]`; dense rows are `[]float32`, sparse rows are `splade.SparseVector` | 03-01-01, 03-01-02 | COVERED |
| CONTEXT | D-02 | `T any`; no tag, switch, wrapper, coercion, or copy | 03-01-01, 03-01-02 | COVERED |
| CONTEXT | D-03 | No heterogeneous runtime registry or weakened non-generic collection | 03-01-01 | COVERED |
| CONTEXT | D-04 | Exact document/query/close interface method set | 03-01-01, 03-01-02 | COVERED |
| CONTEXT | D-05 | MiniLM and SPLADE conform without duplicated or renamed methods | 03-01-02 | COVERED |
| CONTEXT | D-06 | OpenCLIP document/query methods forward to existing text methods | 03-01-01, 03-01-02 | COVERED |
| CONTEXT | D-07 | Preserve all OpenCLIP text/image methods; image stays concrete-only | 03-01-02, 03-01-03 | COVERED |
| CONTEXT | D-08 | Root `embeddings` is a dependency-light contract, not a facade | 03-01-01 | COVERED |
| CONTEXT | D-09 | All constructors retain concrete pointer return types | 03-01-02 | COVERED |
| CONTEXT | D-10 | Existing result types remain package-owned without aliases or moves | 03-01-02 | COVERED |
| CONTEXT | D-11 | Existing exported signatures and behavior remain unchanged | 03-01-02, 03-01-03 | COVERED |
| CONTEXT | D-12 | No factories, adapters, result unions, or capability packages | 03-01-01, 03-01-03 | COVERED |
| CONTEXT | D-13 | Preserve exact validated MiniLM/OpenCLIP/SPLADE conformance without conversion | 03-01-02 | COVERED |
| CONTEXT | D-14 | Root production package has zero imports | 03-01-01, 03-01-03 | COVERED |
| CONTEXT | D-15 | OpenCLIP's only additions are the two direct forwarders | 03-01-01, 03-01-02 | COVERED |
| CONTEXT | D-16 | Pin signatures and run conformance, vet, embedding regressions, and full short suite | 03-01-02, 03-01-03 + final phase verification | COVERED |

Deferred/excluded runtime registries, factories, adapters, result unions or moves, constructor changes, new dependencies, and unrelated cleanup do not appear in this plan.
</source_coverage>

<tasks>

<task type="auto">
  <name>03-01-01: Promote the dependency-free contract and OpenCLIP forwarders</name>
  <files>embeddings/embedder.go, embeddings/openclip/generalized_embedder.go</files>
  <read_first>
    - .planning/phases/03-generalized-embedder-api/03-CONTEXT.md (locked D-01 through D-15 and excluded scope)
    - .planning/phases/03-generalized-embedder-api/03-PATTERNS.md (exact three-file assignments and do-not-modify list)
    - .planning/spikes/003-generic-embedder-contract-proof/overlay/embeddings/embedder.go (validated root-file blueprint)
    - .planning/spikes/003-generic-embedder-contract-proof/overlay/embeddings/openclip/generalized_embedder.go (validated forwarder-file blueprint)
    - .planning/spikes/003-generic-embedder-contract-proof/README.md (positive proof, negative control, and limits)
    - embeddings/minilm/embedder.go (existing dense method set; preserve without editing)
    - embeddings/splade/embedder.go (existing sparse method set and package-owned result types; preserve without editing)
    - embeddings/openclip/embedder.go (existing `EmbedTexts`, `EmbedText`, image methods, validation, locking, caching, and cleanup; preserve without editing)
    - .planning/phases/02-core-api-errors-values/02-CONTEXT.md (settled `Value`, `Run`, ownership, lifetime, and lock behavior; preserve without editing `ort/`)
    - go.mod (Go 1.25 baseline and unchanged dependency graph)
  </read_first>
  <action>
    Create `embeddings/embedder.go` from the validated Spike 003 shape. Per D-01, D-02, D-04, D-08, and D-14, declare package `embeddings`, give the package and exported interface concise GoDoc, and define only `Embedder[T any]` with `EmbedDocuments(documents []string) ([]T, error)`, `EmbedQuery(query string) (T, error)`, and `Close() error`. Keep the file free of imports and implementation code. Per D-03 and D-12, do not add a registry, factory, adapter, result tag/union, type switch, conversion, alias, or model-specific capability.

    Create `embeddings/openclip/generalized_embedder.go` from its validated overlay. Per D-06 and D-15, add `(*Embedder).EmbedDocuments([]string) ([][]float32, error)` as one direct return of `e.EmbedTexts(documents)` and `(*Embedder).EmbedQuery(string) ([]float32, error)` as one direct return of `e.EmbedText(query)`, with normal GoDoc and no imports. Do not add validation, wrapping, allocation, locking, tokenization, inference, or post-processing; the existing target methods remain the sole behavior owners.

    Per D-05, D-07, D-09 through D-11, and D-13, do not edit `embeddings/minilm/embedder.go`, `embeddings/splade/embedder.go`, `embeddings/openclip/embedder.go`, any existing result type, constructor, or `ort/` file. Format both new files with `gofmt` and keep `go.mod`/`go.sum` unchanged.
  </action>
  <verify>
    <automated>test -z "$(gofmt -l embeddings/embedder.go embeddings/openclip/generalized_embedder.go)" &amp;&amp; test -z "$(go list -f '{{join .Imports " "}}' ./embeddings)" &amp;&amp; go test -run '^$' ./embeddings/... &amp;&amp; git diff --exit-code -- go.mod go.sum</automated>
  </verify>
  <acceptance_criteria>
    - `go doc ./embeddings.Embedder` resolves one public `Embedder[T any]` interface with exactly `EmbedDocuments`, `EmbedQuery`, and `Close`.
    - `go list -f '{{join .Imports " "}}' ./embeddings` prints an empty line.
    - `embeddings/openclip/generalized_embedder.go` contains exactly the two exported forwarding methods, and each body is a direct return to `EmbedTexts` or `EmbedText` with no other behavior.
    - `go test -run '^$' ./embeddings/...` exits 0, proving the root and all model packages compile without an import cycle.
    - `git diff --exit-code -- go.mod go.sum` exits 0, and no existing production source file is modified by this task.
  </acceptance_criteria>
  <done>The production package graph contains the exact zero-import generic contract and the two additive OpenCLIP forwarders, with all existing implementations and the settled core untouched.</done>
</task>

<task type="auto">
  <name>03-01-02: Promote exact conformance, signature, and forwarding proofs</name>
  <files>embeddings/embedder_test.go</files>
  <read_first>
    - embeddings/embedder.go (Task 03-01-01 contract under test)
    - embeddings/openclip/generalized_embedder.go (Task 03-01-01 forwarders under test)
    - .planning/spikes/003-generic-embedder-contract-proof/contract_test.go (exact assertion and dispatch-test blueprint)
    - .planning/phases/03-generalized-embedder-api/03-CONTEXT.md (D-01 through D-16 compatibility requirements)
    - .planning/phases/03-generalized-embedder-api/03-PATTERNS.md (`package embeddings_test` adaptation and two added signature pins)
    - embeddings/minilm/embedder.go (constructor/document/query/close signatures to pin)
    - embeddings/splade/embedder.go (`SparseVector`, constructor/document/query/close signatures to pin)
    - embeddings/openclip/embedder.go (constructor, existing text/image signatures, and direct validation behavior to pin)
    - ort/public_api_compat_test.go (repository external-package compatibility-test convention)
    - .planning/phases/02-core-api-errors-values/02-CONTEXT.md (do not alter the package-sealed `Value` or ownership paths)
  </read_first>
  <action>
    Create `embeddings/embedder_test.go` using `package embeddings_test`, adapting the complete Spike 003 contract proof rather than importing child model packages from production. Per D-01, D-05, and D-13, retain compile-time assignments proving `*minilm.Embedder` and `*openclip.Embedder` implement `embeddings.Embedder[[]float32]` and `*splade.Embedder` implements `embeddings.Embedder[splade.SparseVector]` with exact method signatures and no result conversion.

    Per D-07, D-09 through D-11, and D-16, retain every Spike constructor and existing method function-type pin for MiniLM, SPLADE, and OpenCLIP, including OpenCLIP's image methods and all three `Close` methods. Add the two exact new function-type pins for `(*openclip.Embedder).EmbedDocuments` and `(*openclip.Embedder).EmbedQuery`. Do not use reflection or runtime assertions where a compile-time assignment can enforce compatibility.

    Retain the generic query/document dispatch helpers and `TestTypedInterfaceDispatchReachesExistingValidation`. Retain `TestOpenCLIPForwardersPreserveExistingValidation`, comparing direct and forwarded nil-receiver errors from the same call shape without pinning literal error text. This verifies T-03-01 through T-03-03 while leaving all model, fixture, inference, and result files unchanged. Format the new test with `gofmt`.
  </action>
  <verify>
    <automated>test -z "$(gofmt -l embeddings/embedder_test.go)" &amp;&amp; go test -count=1 ./embeddings &amp;&amp; go test -count=1 ./embeddings -run '^(TestTypedInterfaceDispatchReachesExistingValidation|TestOpenCLIPForwardersPreserveExistingValidation)$'</automated>
  </verify>
  <acceptance_criteria>
    - `embeddings/embedder_test.go` uses `package embeddings_test` and is the only new file that imports the root plus all three child embedding packages.
    - Three compile-time assignments prove the exact dense MiniLM, sparse SPLADE, and dense OpenCLIP interface instantiations.
    - Function-type assignments pin all three existing constructors, MiniLM's document/query/close methods, SPLADE's document/query/close methods, OpenCLIP's existing text/image/close methods, and the two new forwarder methods.
    - `TestTypedInterfaceDispatchReachesExistingValidation` exercises MiniLM, SPLADE, and both OpenCLIP contract calls through typed generic helpers without native assets.
    - `TestOpenCLIPForwardersPreserveExistingValidation` proves direct and forwarded query/document calls return matching validation errors without treating a literal message as API.
    - Both automated `go test` commands exit 0.
  </acceptance_criteria>
  <done>API-01 has a permanent compile-time compatibility proof and focused unit proof that generalized dispatch preserves each concrete validation path.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>03-01-03: Make the configured native acceptance environment available</name>
  <files>No files modified; verify embeddings/embedder.go, embeddings/openclip/generalized_embedder.go, embeddings/embedder_test.go, and the existing MiniLM/SPLADE/OpenCLIP test suites.</files>
  <read_first>
    - .planning/phases/03-generalized-embedder-api/03-VALIDATION.md (canonical T-03-01 through T-03-05 commands and PASS-not-SKIP rule)
    - .planning/phases/03-generalized-embedder-api/03-RESEARCH.md (Validation Architecture and configured-environment limits)
    - .planning/spikes/003-generic-embedder-contract-proof/README.md (validated short-suite/vet baseline and native proof limit)
    - embeddings/embedder.go, embeddings/openclip/generalized_embedder.go, embeddings/embedder_test.go (final Phase 3 delta)
    - embeddings/minilm/embedder_integration_test.go (dense document/query and session-reuse gate)
    - embeddings/splade/embedder_integration_test.go (sparse model gate and asset configuration)
    - embeddings/splade/golden_regression_test.go (local sparse golden/repeatability gates)
    - embeddings/splade/golden_dataset_parity_test.go (hosted sparse parity and skip conditions)
    - embeddings/openclip/embedder_integration_test.go (text/image, validation, close, and lifecycle gates)
    - embeddings/openclip/golden_dataset_parity_test.go (hosted text/image parity and skip conditions)
    - TESTING.md (documented `ONNXRUNTIME_LIB_PATH`, model, tokenizer, and golden dataset configuration)
    - go.mod and go.sum (must remain unchanged)
  </read_first>
  <action>
    First run the focused root contract sample in `<verify>`; it is the under-30-second task-level feedback check. Then pause because RESEARCH.md records `ONNXRUNTIME_LIB_PATH`, model assets, and golden configuration as unavailable in the current shell. Per D-11, D-12, and D-16, do not edit production code, integration/golden tests, fixtures, module files, or workflow selection to bypass this environment requirement.
  </action>
  <verify>
    <automated>go test -count=1 ./embeddings -run '^(TestTypedInterfaceDispatchReachesExistingValidation|TestOpenCLIPForwardersPreserveExistingValidation)$'</automated>
  </verify>
  <instructions>
    Make one configured native execution target available to the executor; do not run the tests manually. Use either the existing `integration-real-model` CI environment for the current revision or an accessible Linux shell matching `.github/workflows/ci.yml`: Go 1.25.x, verified ONNX Runtime 1.24.1, and the pinned MiniLM, SPLADE, OpenCLIP, and hosted-golden configuration. The executor will run the comprehensive final phase verification after access is available.
  </instructions>
  <verification>After resume, the executor confirms the configured target uses the pinned CI contract, then runs the plan-level `<verification><automated>` gate. Any missing named PASS, any named SKIP, or any nonzero test exit blocks completion.</verification>
  <resume-signal>Type "ready" and identify the configured CI run target or accessible configured shell.</resume-signal>
  <done>A configured native environment for the current revision is available to the executor; no test or production source was weakened to obtain it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller generic type argument → concrete embedder method set | The selected row type must match the implementation exactly at compile time; no runtime tag or conversion may weaken the boundary. |
| Generalized OpenCLIP names → existing text inference methods | New public names must preserve existing receiver, closed-state, runtime, validation, locking, and inference behavior. |
| Root contract package → model implementation packages | The production dependency direction must remain one-way: model packages may be tested against the root contract, while the root imports none of them. |
| Native runtime/model/dataset → regression evidence | Environment-dependent tests can skip; the phase gate must distinguish real named PASS evidence from a green skipped package. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering / Denial of Service | HIGH | OpenCLIP forwarding names | mitigate | Task 03-01-01 uses only direct delegation; Task 03-01-02 compares direct and forwarded validation; the configured final phase gate after Task 03-01-03 runs unchanged OpenCLIP integration/parity tests and requires named PASS evidence. |
| T-03-02 | Tampering | HIGH | Dense/sparse result typing | mitigate | Tasks 03-01-01/02 retain `T any` and exact compile-time assertions for `[]float32` and `splade.SparseVector`; no tags, unions, coercion, or conversion are permitted. |
| T-03-03 | Denial of Service | HIGH | Explicit embedder cleanup | mitigate | The Task 03-01-01 interface includes `Close() error`; Task 03-01-02 pins each concrete `Close` signature and requires all three implementations to conform. |
| T-03-04 | Denial of Service / Supply-chain expansion | MEDIUM | Root package dependency boundary | mitigate | Task 03-01-01 adds only the three-method zero-import contract; the final phase gate after Task 03-01-03 fails unless `go list` reports no production imports and module files remain unchanged. |
| T-03-05 | Denial of Service | MEDIUM | Existing constructors, result types, and model-specific methods | mitigate | Task 03-01-02 compile-pins the complete existing public surface; the final phase gate after Task 03-01-03 runs short, vet, dense/sparse regression, and native parity gates without modifying fixtures or implementations. |

No HIGH threat is accepted or unresolved. No package install occurs, so the package-legitimacy gate and a supply-chain install checkpoint are not applicable.
</threat_model>

<verification>
1. Run Tasks 03-01-01 and 03-01-02 and the fast Task 03-01-03 `<automated>` sample in order. Stop at Task 03-01-03 until the configured native environment checkpoint is satisfied.
2. Confirm `go doc ./embeddings.Embedder` exposes exactly the three locked methods and `go list` reports zero root imports.
3. Confirm the diff adds only `embeddings/embedder.go`, `embeddings/openclip/generalized_embedder.go`, and `embeddings/embedder_test.go`; `go.mod`, `go.sum`, existing embedder sources/tests/fixtures, and `ort/` remain unchanged.
4. After the checkpoint resumes, run the comprehensive automated phase gate below in the configured native environment. Preserve its JSON logs in execution evidence: all 12 named native tests must have exact top-level PASS events and none may have a SKIP event.
5. Run `git diff --check` before committing the implementation.

<automated>set -eu
set -o pipefail
test -z "$(gofmt -l embeddings/embedder.go embeddings/openclip/generalized_embedder.go embeddings/embedder_test.go)"
test -z "$(go list -f '{{join .Imports " "}}' ./embeddings)"
go test -count=1 ./embeddings
go test -count=1 -short ./embeddings/...
go vet ./embeddings/...
go test -count=1 -short ./...
git diff --exit-code -- go.mod go.sum
phase03_run_gate() {
  phase03_expected="$1"
  phase03_pattern="$2"
  shift 2
  phase03_log="$(mktemp)"
  "$@" | tee "$phase03_log"
  phase03_pass_count="$(grep -Ec "\"Action\":\"pass\".*\"Test\":\"(${phase03_pattern})\"" "$phase03_log")"
  test "$phase03_pass_count" -eq "$phase03_expected"
  if grep -Eq "\"Action\":\"skip\".*\"Test\":\"(${phase03_pattern})\"" "$phase03_log"; then
    return 1
  fi
}
phase03_run_gate 1 'TestEmbedDocumentsWithAllMiniLML6V2' go test -count=1 -json ./embeddings/minilm -run '^TestEmbedDocumentsWithAllMiniLML6V2$'
phase03_run_gate 3 'TestEmbedDocumentsWithSPLADEModel|TestSPLADEGoldenRegressionTopK16WithLabels|TestSPLADERepeatabilityTopK16' go test -count=1 -json ./embeddings/splade -run '^(TestEmbedDocumentsWithSPLADEModel|TestSPLADEGoldenRegressionTopK16WithLabels|TestSPLADERepeatabilityTopK16)$'
phase03_run_gate 1 'TestSPLADEGoldenDatasetParity' go test -count=1 -json ./embeddings/splade -run '^TestSPLADEGoldenDatasetParity$'
phase03_run_gate 6 'TestEmbedTextsAndImagesWithOpenCLIPModel|TestOpenCLIPFailsWithWrongInputOutputNames|TestOpenCLIPFailsWithWrongEmbeddingDimension|TestOpenCLIPFailsWithImageSizeMismatch|TestOpenCLIPErrorsAfterClose|TestOpenCLIPCloseIsIdempotent' go test -count=1 -json ./embeddings/openclip -run '^(TestEmbedTextsAndImagesWithOpenCLIPModel|TestOpenCLIPFailsWithWrongInputOutputNames|TestOpenCLIPFailsWithWrongEmbeddingDimension|TestOpenCLIPFailsWithImageSizeMismatch|TestOpenCLIPErrorsAfterClose|TestOpenCLIPCloseIsIdempotent)$'
phase03_run_gate 1 'TestOpenCLIPGoldenDatasetParity' go test -count=1 -json ./embeddings/openclip -run '^TestOpenCLIPGoldenDatasetParity$'</automated>
</verification>

<success_criteria>
- API-01 is satisfied by one statically typed `Embedder[T any]` family that accepts existing dense MiniLM/OpenCLIP and sparse SPLADE implementations.
- OpenCLIP gains only `EmbedDocuments` and `EmbedQuery`, both direct forwards, while its existing text/image surface and behavior remain unchanged.
- The production root package has zero imports and contains no factory, registry, adapter, result union, model-specific type, or inference code.
- Compile-time tests pin all three conformance relationships and every old/new signature required by D-16.
- Root, embedding short, vet, full short-module, MiniLM native, SPLADE regression/parity, and OpenCLIP integration/parity gates pass, with exact named PASS and zero SKIP evidence for native selectors.
- D-01 through D-16 and canonical threats T-03-01 through T-03-05 are fully covered; every HIGH threat is mitigated by a concrete task and automated test.
</success_criteria>

<output>
Create `.planning/phases/03-generalized-embedder-api/03-01-SUMMARY.md` when done.
</output>
