---
phase: 03-generalized-embedder-api
plan: 01
type: execute
wave: 1
depends_on: []
# Implementation diff only. Executor evidence and the completion SUMMARY are planning metadata, not source changes.
files_modified:
  - embeddings/embedder.go
  - embeddings/openclip/generalized_embedder.go
  - embeddings/embedder_test.go
autonomous: false
requirements: [API-01]

must_haves:
  truths:
    - "Go callers can use one compile-time `embeddings.Embedder[T any]` call shape for dense MiniLM/OpenCLIP rows and sparse SPLADE rows without a runtime registry, tag, union, coercion, or copy (D-01 through D-04)."
    - "MiniLM and SPLADE satisfy the shared interface through their unchanged method sets, concrete constructors, package-owned result types, inference paths, and cleanup behavior (D-05, D-09 through D-11, D-13)."
    - "OpenCLIP satisfies `embeddings.Embedder[[]float32]` through two direct text forwarders while its existing text and image methods remain concrete and unchanged (D-06, D-07, D-09, D-11, D-15)."
    - "The root production `embeddings` package is a zero-import contract only: it exposes no factory, adapter, registry, result union, alias, model-specific capability, or inference code (D-02, D-03, D-08, D-10, D-12, D-14)."
    - "Compile-time assertions pin every required conformance and existing signature, while native selector evidence proves the unchanged real-model and golden behavior with exact named PASS counts and zero named SKIPs (D-11, D-13, D-16)."
  artifacts:
    - path: "embeddings/embedder.go"
      provides: "Public dependency-free `Embedder[T any]` contract"
      contains: "type Embedder[T any] interface"
    - path: "embeddings/openclip/generalized_embedder.go"
      provides: "Additive OpenCLIP document/query names that delegate to the existing text API"
      exports: ["EmbedDocuments", "EmbedQuery"]
    - path: "embeddings/embedder_test.go"
      provides: "External-package exact-signature, generic-conformance, typed-dispatch, and forwarding proof"
      contains: "package embeddings_test"
  key_links:
    - from: "embeddings/embedder_test.go"
      to: "embeddings/embedder.go"
      via: "compile-time assignments for dense and sparse interface instantiations"
      pattern: "embeddings\\.Embedder\\[(\\[\\]float32|splade\\.SparseVector)\\]"
    - from: "embeddings/openclip/generalized_embedder.go"
      to: "embeddings/openclip/embedder.go"
      via: "single direct returns to EmbedTexts and EmbedText"
      pattern: "return e\\.Embed(Texts|Text)\\("
    - from: "embeddings/embedder_test.go"
      to: "embeddings/minilm, embeddings/splade, embeddings/openclip"
      via: "exact constructor/method pins and typed interface dispatch"
      pattern: "NewEmbedder|EmbedDocuments|EmbedQuery|EmbedTexts|EmbedText|EmbedImages|EmbedImage|Close"
---

<objective>
Promote the spike-validated generalized embedder contract into the production package graph and prove it preserves every concrete embedding API and result path.

Purpose: API-01 needs one statically typed text-embedding surface for dense and sparse rows, including SPLADE, without disturbing the settled Phase 2 core or adding runtime abstraction machinery.
Output: a zero-import root generic interface, two direct OpenCLIP forwarding methods, external-package compatibility proof, and revision-bound native JSON evidence.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-generalized-embedder-api/03-CONTEXT.md
@.planning/phases/03-generalized-embedder-api/03-RESEARCH.md
@.planning/phases/03-generalized-embedder-api/03-PATTERNS.md
@.planning/phases/03-generalized-embedder-api/03-VALIDATION.md
@.planning/phases/03-generalized-embedder-api/03-REVIEWS.md
@.planning/phases/02-core-api-errors-values/02-CONTEXT.md
@.planning/phases/02-core-api-errors-values/02-02-SUMMARY.md
@.planning/phases/02-core-api-errors-values/02-04-SUMMARY.md
@.planning/spikes/003-generic-embedder-contract-proof/README.md
@.planning/spikes/003-generic-embedder-contract-proof/overlay/embeddings/embedder.go
@.planning/spikes/003-generic-embedder-contract-proof/overlay/embeddings/openclip/generalized_embedder.go
@.planning/spikes/003-generic-embedder-contract-proof/contract_test.go
@embeddings/minilm/embedder.go
@embeddings/splade/embedder.go
@embeddings/openclip/embedder.go
@embeddings/minilm/embedder_integration_test.go
@embeddings/splade/golden_dataset_parity_test.go
@embeddings/splade/golden_regression_test.go
@embeddings/openclip/golden_dataset_parity_test.go
@.github/workflows/ci.yml
</context>

<interfaces>
<!-- Spike 003 compiled these exact contracts. Implement and pin them; do not redesign them. -->

From `.planning/spikes/003-generic-embedder-contract-proof/overlay/embeddings/embedder.go`:

```go
type Embedder[T any] interface {
	EmbedDocuments(documents []string) ([]T, error)
	EmbedQuery(query string) (T, error)
	Close() error
}
```

Existing signatures that must remain unchanged:

```go
func (*minilm.Embedder).EmbedDocuments([]string) ([][]float32, error)
func (*minilm.Embedder).EmbedQuery(string) ([]float32, error)
func (*minilm.Embedder).Close() error

func (*splade.Embedder).EmbedDocuments([]string) ([]splade.SparseVector, error)
func (*splade.Embedder).EmbedQuery(string) (splade.SparseVector, error)
func (*splade.Embedder).Close() error

func (*openclip.Embedder).EmbedTexts([]string) ([][]float32, error)
func (*openclip.Embedder).EmbedText(string) ([]float32, error)
func (*openclip.Embedder).EmbedImages([]image.Image) ([][]float32, error)
func (*openclip.Embedder).EmbedImage(image.Image) ([]float32, error)
func (*openclip.Embedder).Close() error
```

The only new OpenCLIP signatures are direct forwarding methods:

```go
func (*openclip.Embedder).EmbedDocuments([]string) ([][]float32, error)
func (*openclip.Embedder).EmbedQuery(string) ([]float32, error)
```
</interfaces>

<dependency_graph>
- 03-01-01 needs the validated Spike 003 blueprints and creates the zero-import root contract plus the complete OpenCLIP text-conformance method set.
- 03-01-02 needs 03-01-01, creates the permanent external conformance/signature proof, and leaves the three implementation paths committed and clean for revision-bound native evidence.
- 03-01-03 needs 03-01-02. It checks the exact committed revision and native target before any named selector; it then blocks only if the executor cannot access a matching configured target.
</dependency_graph>

<tasks>

<task type="auto">
  <name>Task 1: Add the leaf generic contract and direct OpenCLIP text forwarders</name>
  <files>embeddings/embedder.go, embeddings/openclip/generalized_embedder.go</files>
  <read_first>
    - `.planning/phases/03-generalized-embedder-api/03-CONTEXT.md` for D-01 through D-15 and the explicit exclusions.
    - `.planning/spikes/003-generic-embedder-contract-proof/overlay/embeddings/embedder.go` and `overlay/embeddings/openclip/generalized_embedder.go` for the compiled production shape.
    - `embeddings/openclip/embedder.go` for existing `EmbedTexts`, `EmbedText`, text/image methods, receiver behavior, and package style.
    - `embeddings/minilm/embedder.go` and `embeddings/splade/embedder.go` to confirm their existing method sets are not edited.
    - `embeddings/embedder.go` and `embeddings/openclip/generalized_embedder.go` after creation as the modified source-of-truth files.
  </read_first>
  <action>
    Create `embeddings/embedder.go` as the root `embeddings` package's only production contract: package documentation plus `type Embedder[T any] interface` with exactly `EmbedDocuments(documents []string) ([]T, error)`, `EmbedQuery(query string) (T, error)`, and `Close() error` (D-01, D-02, D-04, D-08, D-14). It must have no imports, model references, factory, registry, adapter, result wrapper, alias, tag, coercion, copy, or inference code (D-02, D-03, D-10, D-12). Create `embeddings/openclip/generalized_embedder.go` in package `openclip` with only `(*Embedder).EmbedDocuments`, returning `e.EmbedTexts(documents)`, and `(*Embedder).EmbedQuery`, returning `e.EmbedText(query)` (D-06, D-15). Preserve every existing OpenCLIP text/image method and every MiniLM/SPLADE file, constructor, result type, session path, ownership rule, and behavior unchanged (D-05, D-07, D-09 through D-11). Do not add dependencies or modify module files.
  </action>
  <verify>
    <automated>test -z "$(gofmt -l embeddings/embedder.go embeddings/openclip/generalized_embedder.go)" &amp;&amp; test -z "$(go list -f '{{join .Imports " "}}' ./embeddings)" &amp;&amp; go test -count=1 -run '^$' ./embeddings/... &amp;&amp; git diff --check -- embeddings/embedder.go embeddings/openclip/generalized_embedder.go</automated>
  </verify>
  <acceptance_criteria>
    - `embeddings/embedder.go` has zero imports and exactly the three locked interface methods with unconstrained `T any`.
    - `embeddings/openclip/generalized_embedder.go` contains exactly two public methods, each a direct single return to its existing text method.
    - No MiniLM, SPLADE, existing OpenCLIP, `ort/`, module, fixture, or workflow file changes are introduced.
  </acceptance_criteria>
  <done>The two production files compile, are formatted, preserve the one-way dependency boundary, and implement D-01 through D-15 without expanding scope.</done>
</task>

<task type="auto">
  <name>Task 2: Add external conformance and exact-signature regression proof</name>
  <files>embeddings/embedder_test.go</files>
  <read_first>
    - `embeddings/embedder.go` and `embeddings/openclip/generalized_embedder.go`, the modified production source-of-truth files from Task 1.
    - `.planning/spikes/003-generic-embedder-contract-proof/contract_test.go` for the validated assertions and direct-vs-forwarded validation checks.
    - `embeddings/minilm/embedder.go`, `embeddings/splade/embedder.go`, and `embeddings/openclip/embedder.go` for every constructor/method type that must remain pinned.
    - `.planning/phases/03-generalized-embedder-api/03-VALIDATION.md` and `03-REVIEWS.md` for native selector counts, zero-SKIP policy, and evidence requirements.
    - `embeddings/embedder_test.go` after creation as the modified source-of-truth file.
  </read_first>
  <behavior>
    - Compile-time conformance: `*minilm.Embedder` and `*openclip.Embedder` satisfy `embeddings.Embedder[[]float32]`; `*splade.Embedder` satisfies `embeddings.Embedder[splade.SparseVector]` (D-01, D-13).
    - Exact API compatibility: the three constructors; MiniLM/SPLADE `EmbedDocuments`, `EmbedQuery`, and `Close`; OpenCLIP `EmbedTexts`, `EmbedText`, `EmbedImages`, `EmbedImage`, and `Close` retain their exact function types (D-07, D-09 through D-11, D-16).
    - Typed dispatch: generic calls through the common interface reach each nil concrete embedder's existing validation error rather than succeeding or coercing a result (D-02, D-05, D-13).
    - Forwarding: on a nil OpenCLIP embedder, each new method returns the same non-nil validation error text as its direct existing text-method counterpart (D-06, D-15).
  </behavior>
  <action>
    Add `embeddings/embedder_test.go` with `package embeddings_test`, importing the root contract and child model packages only from the external test package so production `embeddings` remains import-free (D-08, D-14). Transfer Spike 003's three generic interface assignments and all exact function-type assignments, including OpenCLIP image methods, then retain generic helper calls and the two nil-receiver validation comparisons (D-07, D-11, D-13, D-15, D-16). The test must make no runtime result tag, reflection, wrapper, conversion, copy, adapter, factory, registry, model-result migration, or fixture change (D-02, D-03, D-10, D-12). Before this task finishes, commit the implementation/test work according to the execute workflow and confirm the three `files_modified` paths have no unstaged, staged, or untracked changes; Task 3 must test that committed `HEAD`, never an uncommitted source tree.
  </action>
  <verify>
    <automated>go test -count=1 ./embeddings &amp;&amp; go vet ./embeddings/... &amp;&amp; go doc ./embeddings Embedder &amp;&amp; git diff --quiet -- embeddings/embedder.go embeddings/openclip/generalized_embedder.go embeddings/embedder_test.go &amp;&amp; git diff --cached --quiet -- embeddings/embedder.go embeddings/openclip/generalized_embedder.go embeddings/embedder_test.go &amp;&amp; test -z "$(git ls-files --others --exclude-standard -- embeddings/embedder.go embeddings/openclip/generalized_embedder.go embeddings/embedder_test.go)"</automated>
  </verify>
  <acceptance_criteria>
    - The test is an external package and contains all three instantiated interface assertions plus every Spike 003 constructor/method signature pin.
    - The two forwarding assertions compare direct and forwarded OpenCLIP validation errors, while OpenCLIP image methods remain exact-signature-only concrete capabilities.
    - `go doc ./embeddings Embedder` succeeds and exposes the intended generic contract.
    - The implementation paths are clean and available from committed `HEAD`; native evidence cannot accidentally describe uncommitted source changes.
  </acceptance_criteria>
  <done>API-01's compile-time compatibility proof passes, and all implementation paths exist in a clean committed revision ready for native verification.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Verify an exact-revision native target before running the named selector gate</name>
  <files>.planning/phases/03-generalized-embedder-api/evidence/03-01-native-${REVISION}-{manifest,01-minilm,02-splade-regression,03-splade-golden,04-openclip-integration,05-openclip-golden} (executor evidence only; excluded from implementation `files_modified`)</files>
  <read_first>
    - `embeddings/embedder.go`, `embeddings/openclip/generalized_embedder.go`, and `embeddings/embedder_test.go`, the committed implementation source-of-truth files.
    - `.github/workflows/ci.yml` `integration-real-model` job and `.planning/phases/03-generalized-embedder-api/03-VALIDATION.md` for the configured target and five selector contracts.
    - `.planning/phases/03-generalized-embedder-api/03-REVIEWS.md` for the target-availability, durable-evidence, revision-binding, and zero-PASS failure requirements.
    - `embeddings/minilm/embedder_integration_test.go`, `embeddings/splade/golden_dataset_parity_test.go`, `embeddings/splade/golden_regression_test.go`, and `embeddings/openclip/golden_dataset_parity_test.go` for the unchanged native tests that will be selected.
  </read_first>
  <what-built>Tasks 1 and 2 added the only three implementation/test files and committed them. The executor has also run the root contract test, embedding vet, and GoDoc check; the remaining gate is five environment-dependent real-model/golden selectors.</what-built>
  <action>
    Before running any named native selector, the executor must capture `git rev-parse HEAD` and confirm the three implementation paths are clean, tracked by that revision, and not merely present in the worktree. It must then prove one command-capable native target can run the five `go test -json` selectors at that exact revision: either an accessible Linux `amd64`, Go `1.25.x` shell carrying the `integration-real-model` runtime/model/golden configuration, or a project CI target whose exact run and command-capable shell can execute and return those JSON streams. A CI run that exposes only the workflow's current `go test -v` text is not sufficient JSON evidence. Set a non-secret `PHASE03_NATIVE_TARGET_ID` such as `shell:configured-linux-amd64` or `ci:integration-real-model:RUN_ID`; do not place credentials, URLs with tokens, or private host details in the manifest. If no such current-revision target is accessible, do not run or weaken tests and do not claim phase completion; report the missing target and wait for this checkpoint's resume signal. Addresses review concerns: early usable-target check, current-revision binding, and practical blocked-path evidence.
  </action>
  <verify>
    <automated>phase03_revision="$(git rev-parse HEAD)" &amp;&amp; git diff --quiet -- embeddings/embedder.go embeddings/openclip/generalized_embedder.go embeddings/embedder_test.go &amp;&amp; git diff --cached --quiet -- embeddings/embedder.go embeddings/openclip/generalized_embedder.go embeddings/embedder_test.go &amp;&amp; test -z "$(git ls-files --others --exclude-standard -- embeddings/embedder.go embeddings/openclip/generalized_embedder.go embeddings/embedder_test.go)" &amp;&amp; git cat-file -e "$phase03_revision:embeddings/embedder.go" &amp;&amp; git cat-file -e "$phase03_revision:embeddings/openclip/generalized_embedder.go" &amp;&amp; git cat-file -e "$phase03_revision:embeddings/embedder_test.go"</automated>
  </verify>
  <how-to-verify>
    1. Provide the executor access to one target that has the CI job's Linux/Go/ONNX Runtime/model/golden configuration and can run commands against the committed `HEAD` reported by the automated check.
    2. Supply a non-secret target/run identifier through `PHASE03_NATIVE_TARGET_ID`. For a CI target, include the `integration-real-model` run identifier; for a shell, use a stable generic label rather than a private host name.
    3. Do not run tests manually. After access is available, the executor runs the final automated gate below, writes its five JSON streams and revision manifest under `evidence/`, and reports the recorded revision and target identifier.
  </how-to-verify>
  <acceptance_criteria>
    - The configured target is proven available before a named selector starts and is capable of producing the five required JSON streams for committed `HEAD`.
    - The target has a non-secret identifier and all three implementation paths are clean and retrievable from the exact revision the executor will record.
    - If target availability cannot be proven, execution is blocked at this checkpoint without editing production/tests, bypassing selectors, accepting SKIP output, or claiming completion.
  </acceptance_criteria>
  <resume-signal>Type `ready: &lt;non-secret target or run identifier&gt;` after the current committed revision is available in a command-capable configured native target.</resume-signal>
  <done>A current-revision configured native target is available, or execution remains correctly blocked with the precise missing-access condition recorded.</done>
</task>

</tasks>

<expected_outputs>
## Implementation diff (the only entries in `files_modified`)

- `embeddings/embedder.go`
- `embeddings/openclip/generalized_embedder.go`
- `embeddings/embedder_test.go`

## Executor-produced evidence (not implementation files; create only after the committed-revision preflight)

For `REVISION=$(git rev-parse HEAD)`, the final gate must create these durable files under `.planning/phases/03-generalized-embedder-api/evidence/`:

- `03-01-native-${REVISION}-manifest.env` — `GIT_REVISION`, `PHASE03_NATIVE_TARGET_ID`, Go version, GOOS/GOARCH, and an `ONNXRUNTIME_LIB_PATH` presence indicator.
- `03-01-native-${REVISION}-01-minilm.jsonl`
- `03-01-native-${REVISION}-02-splade-regression.jsonl`
- `03-01-native-${REVISION}-03-splade-golden.jsonl`
- `03-01-native-${REVISION}-04-openclip-integration.jsonl`
- `03-01-native-${REVISION}-05-openclip-golden.jsonl`

Only files whose filename revision matches the manifest's `GIT_REVISION` and the tested `HEAD` are valid evidence. Existing evidence for another revision must never satisfy this gate. `.planning/phases/03-generalized-embedder-api/03-01-SUMMARY.md` is also executor-produced planning metadata and is intentionally excluded from `files_modified`.
</expected_outputs>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller generic type argument → concrete embedder method set | The selected row type must match the implementation exactly at compile time; no runtime tag or conversion may weaken the boundary. |
| Generalized OpenCLIP names → existing text inference methods | New public names must preserve existing receiver, closed-state, runtime, validation, locking, and inference behavior. |
| Root contract package → model implementation packages | The production dependency direction must remain one-way: model packages may be tested against the root contract, while the root imports none of them. |
| Native runtime/model/dataset → revision-bound evidence | Environment-dependent tests can skip; phase completion requires durable named PASS evidence from an available configured target at the exact committed revision. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering / Denial of Service | HIGH | OpenCLIP forwarding names | mitigate | Task 1 permits only direct delegation; Task 2 compares direct and forwarded validation; the final gate runs unchanged OpenCLIP selectors and requires named PASS evidence. |
| T-03-02 | Tampering | HIGH | Dense/sparse result typing | mitigate | Tasks 1/2 retain `T any` with exact compile-time assertions for `[]float32` and `splade.SparseVector`; tags, unions, coercion, and conversion are forbidden. |
| T-03-03 | Denial of Service | HIGH | Explicit embedder cleanup | mitigate | Task 1 requires `Close() error`; Task 2 pins every concrete `Close` signature and all three interface conformance relations. |
| T-03-04 | Denial of Service / Supply-chain expansion | MEDIUM | Root package dependency boundary | mitigate | Task 1 adds only a three-method zero-import contract; final automated checks fail on any root import or module-file change. |
| T-03-05 | Denial of Service | MEDIUM | Existing constructors, result types, and model-specific methods | mitigate | Task 2 pins the complete existing public surface; the final gate runs short, vet, dense/sparse regression, and native parity tests without changing fixtures or implementations. |

No HIGH threat is accepted or unresolved. No package install occurs, so the package-legitimacy gate and supply-chain install checkpoint are not applicable.
</threat_model>

<source_coverage>
## Multi-source coverage audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Unified dense/sparse API including SPLADE on top of Phase 2 | Tasks 1/2 implement and prove `Embedder[T]`; Tasks 2/3 preserve existing inference and native behavior. |
| REQ | API-01 generalized dense/sparse embedder API | Plan frontmatter and Tasks 1/2; all three embedders conform with SPLADE retaining `SparseVector`. |
| RESEARCH | Spike-proven three-file shape, zero-import leaf, direct OpenCLIP forwarding, external package proof, no new dependency | Tasks 1/2 use the spike files directly; final gate rejects imports/module changes. |
| RESEARCH | Existing MiniLM/SPLADE/OpenCLIP integration, golden, vet, and short-suite gates | Task 3 and the final automated gate run the exact named selectors plus vet and full short suite. |
| CONTEXT | D-01/D-02/D-04 `Embedder[T any]` and exact three methods | Task 1 contract; Task 2 conformance proof. |
| CONTEXT | D-03/D-08/D-10/D-12/D-14 no registry/facade/result migration/factory/imports | Task 1 scope limits and zero-import check; Task 2 external test package. |
| CONTEXT | D-05/D-09/D-11 MiniLM/SPLADE/constructors/results stay unchanged | Tasks 1/2 preserve sources and exact signature pins; final diff/module checks. |
| CONTEXT | D-06/D-07/D-15 OpenCLIP text forwarders, existing text/image methods remain concrete | Task 1 direct methods; Task 2 exact pins and direct-vs-forwarded validation. |
| CONTEXT | D-13/D-16 spike conformance, vet, regression, short module suite | Task 2 transfers proof; final automated gate executes all specified checks. |
| CONTEXT | Deferred/excluded runtime registry, factories, adapters, runtime unions, result migration, capability packages | Explicitly excluded in Tasks 1/2; no task implements a deferred/excluded idea. |

Every in-scope GOAL, REQ, RESEARCH, and CONTEXT item is covered. There are no unplanned source items.
</source_coverage>

<verification>
Run Tasks 1 and 2, then stop at Task 3 if an exact-revision configured native target is unavailable. After the checkpoint resumes, run this complete final gate in that target. It fails for a missing target, nonzero command, dirty/uncommitted implementation paths, a missing or different revision record, a missing/empty durable selector log, any named SKIP, or an incorrect named PASS count. Grep is intentionally retained for this single, tightly scoped JSON-evidence gate; `|| true` preserves the intended zero-PASS diagnostic under `set -e` without adding a reusable parser.

<automated>set -eu
set -o pipefail

phase03_revision="$(git rev-parse HEAD)"
phase03_paths='embeddings/embedder.go embeddings/openclip/generalized_embedder.go embeddings/embedder_test.go'
test -n "${PHASE03_NATIVE_TARGET_ID:-}"
test -n "${ONNXRUNTIME_LIB_PATH:-}"
test -r "$ONNXRUNTIME_LIB_PATH"
test "$(go env GOOS)" = linux
test "$(go env GOARCH)" = amd64
go version | grep -Eq 'go1\\.25\\.'
git diff --quiet -- $phase03_paths
git diff --cached --quiet -- $phase03_paths
test -z "$(git ls-files --others --exclude-standard -- $phase03_paths)"
git cat-file -e "$phase03_revision:embeddings/embedder.go"
git cat-file -e "$phase03_revision:embeddings/openclip/generalized_embedder.go"
git cat-file -e "$phase03_revision:embeddings/embedder_test.go"

test -z "$(gofmt -l $phase03_paths)"
test -z "$(go list -f '{{join .Imports " "}}' ./embeddings)"
go doc ./embeddings Embedder
go test -count=1 ./embeddings
go test -count=1 -short ./embeddings/...
go vet ./embeddings/...
go test -count=1 -short ./...
git diff --exit-code -- go.mod go.sum

phase03_evidence_dir='.planning/phases/03-generalized-embedder-api/evidence'
mkdir -p "$phase03_evidence_dir"
phase03_manifest="$phase03_evidence_dir/03-01-native-${phase03_revision}-manifest.env"
umask 077
{
  printf 'GIT_REVISION=%s\\n' "$phase03_revision"
  printf 'PHASE03_NATIVE_TARGET_ID=%s\\n' "$PHASE03_NATIVE_TARGET_ID"
  printf 'GO_VERSION=%s\\n' "$(go version)"
  printf 'GOOS=%s\\n' "$(go env GOOS)"
  printf 'GOARCH=%s\\n' "$(go env GOARCH)"
  printf 'ONNXRUNTIME_LIB_PATH_SET=true\\n'
} &gt; "$phase03_manifest"
test -s "$phase03_manifest"
grep -Fx "GIT_REVISION=$phase03_revision" "$phase03_manifest"
grep -Fx "PHASE03_NATIVE_TARGET_ID=$PHASE03_NATIVE_TARGET_ID" "$phase03_manifest"

phase03_run_gate() {
  phase03_name="$1"
  phase03_expected="$2"
  phase03_pattern="$3"
  shift 3
  phase03_log="$phase03_evidence_dir/03-01-native-${phase03_revision}-${phase03_name}.jsonl"
  "$@" | tee "$phase03_log"
  test -s "$phase03_log"
  phase03_pass_count="$(grep -Ec "\\\"Action\\\":\\\"pass\\\".*\\\"Test\\\":\\\"(${phase03_pattern})\\\"" "$phase03_log" || true)"
  if [ "$phase03_pass_count" -ne "$phase03_expected" ]; then
    printf 'named PASS count mismatch for %s: got %s, want %s\\n' "$phase03_name" "$phase03_pass_count" "$phase03_expected" &gt;&amp;2
    return 1
  fi
  phase03_skip_count="$(grep -Ec "\\\"Action\\\":\\\"skip\\\".*\\\"Test\\\":\\\"(${phase03_pattern})\\\"" "$phase03_log" || true)"
  if [ "$phase03_skip_count" -ne 0 ]; then
    printf 'named SKIP events found for %s: %s\\n' "$phase03_name" "$phase03_skip_count" &gt;&amp;2
    return 1
  fi
}

phase03_run_gate '01-minilm' 1 'TestEmbedDocumentsWithAllMiniLML6V2' go test -count=1 -json ./embeddings/minilm -run '^TestEmbedDocumentsWithAllMiniLML6V2$'
phase03_run_gate '02-splade-regression' 3 'TestEmbedDocumentsWithSPLADEModel|TestSPLADEGoldenRegressionTopK16WithLabels|TestSPLADERepeatabilityTopK16' go test -count=1 -json ./embeddings/splade -run '^(TestEmbedDocumentsWithSPLADEModel|TestSPLADEGoldenRegressionTopK16WithLabels|TestSPLADERepeatabilityTopK16)$'
phase03_run_gate '03-splade-golden' 1 'TestSPLADEGoldenDatasetParity' go test -count=1 -json ./embeddings/splade -run '^TestSPLADEGoldenDatasetParity$'
phase03_run_gate '04-openclip-integration' 6 'TestEmbedTextsAndImagesWithOpenCLIPModel|TestOpenCLIPFailsWithWrongInputOutputNames|TestOpenCLIPFailsWithWrongEmbeddingDimension|TestOpenCLIPFailsWithImageSizeMismatch|TestOpenCLIPErrorsAfterClose|TestOpenCLIPCloseIsIdempotent' go test -count=1 -json ./embeddings/openclip -run '^(TestEmbedTextsAndImagesWithOpenCLIPModel|TestOpenCLIPFailsWithWrongInputOutputNames|TestOpenCLIPFailsWithWrongEmbeddingDimension|TestOpenCLIPFailsWithImageSizeMismatch|TestOpenCLIPErrorsAfterClose|TestOpenCLIPCloseIsIdempotent)$'
phase03_run_gate '05-openclip-golden' 1 'TestOpenCLIPGoldenDatasetParity' go test -count=1 -json ./embeddings/openclip -run '^TestOpenCLIPGoldenDatasetParity$'

for phase03_name in 01-minilm 02-splade-regression 03-splade-golden 04-openclip-integration 05-openclip-golden; do
  test -s "$phase03_evidence_dir/03-01-native-${phase03_revision}-${phase03_name}.jsonl"
done
test -s "$phase03_manifest"
grep -Fx "GIT_REVISION=$phase03_revision" "$phase03_manifest"
grep -Fx "PHASE03_NATIVE_TARGET_ID=$PHASE03_NATIVE_TARGET_ID" "$phase03_manifest"</automated>
</verification>

<success_criteria>
- API-01 is satisfied by one statically typed `Embedder[T any]` family that accepts existing dense MiniLM/OpenCLIP and sparse SPLADE implementations.
- OpenCLIP gains only `EmbedDocuments` and `EmbedQuery`, both direct forwards, while its existing text/image surface and behavior remain unchanged.
- The production root package has zero imports and contains no factory, registry, adapter, result union, model-specific type, or inference code.
- Compile-time tests pin all three conformance relationships and every old/new signature required by D-16.
- `go doc ./embeddings Embedder`, root test, embedding short suite, vet, full short module suite, and every named native selector pass. The final gate records exactly 12 named test PASS events, zero named SKIP events, the committed tested revision, target identifier, and all five durable JSON logs.
- D-01 through D-16 and canonical threats T-03-01 through T-03-05 are fully covered; every HIGH threat is mitigated by a concrete task and automated test.
</success_criteria>

<output>
After the final gate passes, create `.planning/phases/03-generalized-embedder-api/03-01-SUMMARY.md`. Record the committed `GIT_REVISION`, `PHASE03_NATIVE_TARGET_ID`, and the five revision-specific evidence paths. The SUMMARY and `evidence/` are executor-produced metadata; do not count them as implementation files or use them to justify uncommitted source changes.
</output>
