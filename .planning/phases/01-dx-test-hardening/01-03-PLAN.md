---
phase: 01-dx-test-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ort/environment_stress_test.go
  - .github/workflows/ci.yml
  - TESTING.md
  - Makefile
autonomous: true
requirements: [TST-02]

must_haves:
  truths:
    - "ort/environment_stress_test.go exercises many concurrent InitializeEnvironment/DestroyEnvironment cycles across 3 distinct stress scenarios and passes under go test -race"
    - "Stress tests are skipped by default under go test ./..., make test, and make precommit — testing.Short()'s gate is not cosmetic (RESEARCH.md Pitfall #2, resolved explicitly in this plan)"
    - "A new, separate CI job (test-race-ort-stress) runs the stress tests under -race with -count=50 and a 10-minute timeout, without being merged into the existing test-race-ort-concurrency job's regex"
    - "TESTING.md documents how to run stress tests locally and what they guard against"
  artifacts:
    - path: "ort/environment_stress_test.go"
      provides: "TestStressConcurrentInitDestroy, TestStressRapidInitDestroy, TestStressMixedOperationsUnderLoad — all testing.Short()-gated"
    - path: ".github/workflows/ci.yml"
      provides: "New test-race-ort-stress job; -short added to the existing test job's Unix and Windows steps"
    - path: "TESTING.md"
      provides: "New '### Running Stress Tests' section plus an updated GitHub Actions bullet list"
    - path: "Makefile"
      provides: "-short added to the test: target and precommit's go test ./... step"
  key_links:
    - from: "Makefile test/precommit targets"
      to: "testing.Short()"
      via: "-short flag passed to go test"
      pattern: "test.*-short|-short.*ort"
    - from: ".github/workflows/ci.yml test job (Unix + Windows steps)"
      to: "testing.Short()"
      via: "-short flag added to the default unit-test invocation"
      pattern: "-short"
    - from: ".github/workflows/ci.yml test-race-ort-stress job"
      to: "ort/environment_stress_test.go TestStress* functions"
      via: "go test -v -race -run=TestStress -count=50 -parallel=4 ./ort/... (no -short)"
      pattern: "run=TestStress"
---

<objective>
Add stress coverage for concurrent `InitializeEnvironment`/`DestroyEnvironment` cycles (issue #24 / TST-02), gated behind `testing.Short()` so it does not run in every default `go test ./...` invocation, and give it a dedicated CI job. Explicitly resolve the phase's open question: wire `-short` into the repo's default local/CI test invocations so the `testing.Short()` gate is not cosmetic.

Purpose: Close TST-02's full scope (D-06/D-07/D-08) in one phase, including the CI job, the `-short` wiring the gate depends on to actually mean "skipped by default," and the TESTING.md doc update the user explicitly chose to pull forward from Phase 4.
Output: `ort/environment_stress_test.go` (3 stress tests), a new `test-race-ort-stress` CI job, `-short` added to `Makefile`'s `test`/`precommit` and CI's `test` job, and a new TESTING.md section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-dx-test-hardening/01-CONTEXT.md
@.planning/phases/01-dx-test-hardening/01-RESEARCH.md
@.planning/phases/01-dx-test-hardening/01-PATTERNS.md
</context>

<rationale>
## Resolving the phase's open question: -short wiring IS in scope

CONTEXT.md D-06 requires stress tests "gated behind testing.Short(), so normal go test ./... / CI runs skip them by default." D-08 enumerates CI/TESTING.md changes explicitly but is silent on Makefile's test/precommit targets and the two existing CI unit-test steps. RESEARCH.md's Common Pitfall #2 and Open Question #1, and PATTERNS.md's "Open Question for Planner," both flag that without adding -short somewhere, testing.Short() gates nothing — grepping the repo confirms zero existing -short usage in Makefile or ci.yml.

Decision: -short is added to Makefile's `test:` target and `precommit`'s `go test ./...` step, and to both CI `test` job steps (Unix + Windows) in ci.yml. This is the only way D-06's literal wording ("skip them by default") is satisfied. `make test-race` and the existing `test-race-ort-concurrency` CI job are left unchanged — they already target curated regexes that never match `TestStress*`, so they don't need -short to avoid running the new tests.
</rationale>

<interfaces>
<!-- Foundation pattern to build on (D-07) — ort/environment_test.go, read via read_first for full context. -->

From ort/environment_test.go (TestConcurrentInitialization, lines 243-277 — the exact foundation named in D-07):
```go
func TestConcurrentInitialization(t *testing.T) {
	resetEnvironmentState()
	if err := SetSharedLibraryPath("/nonexistent/path.so"); err != nil {
		t.Fatalf("unexpected error setting library path: %v", err)
	}
	var wg sync.WaitGroup
	concurrency := 10
	mu.Lock()
	refCount = 1
	mu.Unlock()
	for i := 0; i < concurrency; i++ {
		wg.Add(1)
		go func() {
			defer wg.Done()
			_ = InitializeEnvironment()
		}()
	}
	wg.Wait()
	mu.Lock()
	expectedCount := 1 + concurrency
	if refCount != expectedCount {
		t.Errorf("expected refCount to be %d after concurrent inits, got %d", expectedCount, refCount)
	}
	mu.Unlock()
	resetEnvironmentState()
}
```

From ort/environment.go (package-level state these tests exercise, lines 22-51 — `mu`, `refCount`, and the functions under test):
```go
var (
	mu       sync.Mutex
	refCount int
	...
)
func InitializeEnvironment() (err error) { ... }
func DestroyEnvironment() ... { ... }
func IsInitialized() bool { ... }
func GetVersionString() string { ... }
func SetLogLevel(level LoggingLevel) error { ... }
func SetSharedLibraryPath(path string) error { ... }
```

From ort/environment_test.go (resetEnvironmentState fixture, lines 10-32 — call at the start of every new stress test, and via defer for cleanup):
```go
func resetEnvironmentState() {
	mu.Lock()
	defer mu.Unlock()
	refCount = 0
	ortLib = 0
	ortAPI = nil
	...
}
```

Existing sibling CI job to mirror structurally (.github/workflows/ci.yml, test-race-ort-concurrency, lines 108-126):
```yaml
  test-race-ort-concurrency:
    name: Test Race (ORT concurrency subset)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
      - name: Setup Go
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6
        with:
          go-version: ${{ env.GO_VERSION_STABLE }}
          cache: true
      - name: Get dependencies
        run: go mod download
      - name: Run race detector on ORT concurrency tests
        run: |
          go test -race ./ort -run '...'
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ort/environment_stress_test.go with 3 testing.Short()-gated stress tests</name>
  <files>ort/environment_stress_test.go</files>
  <read_first>ort/environment_test.go (lines 1-32 for resetEnvironmentState, lines 243-308 for TestConcurrentInitialization/TestConcurrentDestroy — the explicit D-07 foundation), ort/environment.go (lines 1-120 for mu/refCount/InitializeEnvironment/DestroyEnvironment/IsInitialized/GetVersionString/SetLogLevel/SetSharedLibraryPath signatures)</read_first>
  <behavior>
    - Test: `TestStressConcurrentInitDestroy` — 100 goroutines x 1000 iterations of paired `InitializeEnvironment()`/`DestroyEnvironment()` calls against a nonexistent library path; after `wg.Wait()`, `refCount == 0`.
    - Test: `TestStressRapidInitDestroy` — 200 goroutines x 500 iterations of the same paired calls (different goroutine/iteration shape to stress `mu` contention differently); after `wg.Wait()`, `refCount == 0`.
    - Test: `TestStressMixedOperationsUnderLoad` — 50 goroutines x 500 iterations mixing `InitializeEnvironment()`, `IsInitialized()`, `GetVersionString()`, `DestroyEnvironment()`, and occasional `SetLogLevel(LoggingLevelWarning)` calls (errors from calls invalid under concurrent contention, e.g. `SetLogLevel` after init, are expected and ignored); after `wg.Wait()`, `refCount == 0` and no panic/deadlock occurred.
    - Test: all 3 tests report `SKIP` when run with `-short`.
  </behavior>
  <action>
    Create a new file `ort/environment_stress_test.go`, package `ort`, importing `sync`, `sync/atomic` (only if used), and `testing`. Implement exactly 3 functions, each starting with `if testing.Short() { t.Skip("skipping stress test in short mode") }` followed by `resetEnvironmentState()` and `defer resetEnvironmentState()`, then `SetSharedLibraryPath("/nonexistent/path.so")`:

    1. `TestStressConcurrentInitDestroy` — spawn 100 goroutines (`const goroutines = 100`, `const iterations = 1000`), each looping `iterations` times calling `InitializeEnvironment()` then `DestroyEnvironment()` (ignore returned errors — the invariant under test is refcount balance, not per-call success). After `wg.Wait()`, assert under `mu.Lock()`/`mu.Unlock()` that `refCount == 0`.

    2. `TestStressRapidInitDestroy` — spawn 200 goroutines (`const goroutines = 200`, `const iterations = 500`), each looping `iterations` times calling `InitializeEnvironment()` immediately followed by `DestroyEnvironment()` with no delay — this is a different concurrency shape (more goroutines, fewer iterations each) than test 1, exercising `mu` contention differently per D-06's "at least 2 more" discretion. Same `refCount == 0` assertion after `wg.Wait()`.

    3. `TestStressMixedOperationsUnderLoad` — spawn 50 goroutines (`const goroutines = 50`, `const iterations = 500`), each iteration calling a rotating mix of `InitializeEnvironment()`, `IsInitialized()`, `GetVersionString()`, `DestroyEnvironment()`, and (every Nth iteration, e.g. `if j%10 == 0`) `SetLogLevel(LoggingLevelWarning)` — ignore all returned errors from calls that are expected to fail under contention (e.g. `SetLogLevel` returns an error once `refCount > 0`); the pass condition is that `wg.Wait()` returns without panic/deadlock and `refCount == 0` afterward. This implements issue #24's "Mixed Operations Under Load" scenario as a FIXED-iteration loop, NOT a wall-clock duration (per RESEARCH.md Pitfall #1 — a 10-second wall-clock test x the CI job's `-count=50` would risk exceeding the 10-minute job timeout; fixed iterations keep runtime bounded and deterministic across fast/slow runners).

    Do not add any real FFI/unsafe pointer calls — all three tests use only refcount/mutex logic against a nonexistent library path (same `-race`-safe pattern as `TestConcurrentInitialization`/`TestConcurrentDestroy`, confirmed checkptr-safe by RESEARCH.md's local measurement).
  </action>
  <verify>
    <automated>go test -race -run TestStress -count=5 ./ort/... -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "^func TestStress" ort/environment_stress_test.go equals 3
    - grep -c "testing.Short()" ort/environment_stress_test.go equals 3 (one skip-guard per test)
    - go test -race -run TestStress -count=5 ./ort/... -v passes with zero race warnings and zero panics
    - go test -short -run TestStress ./ort/... -v shows all 3 tests reported as `--- SKIP`
  </acceptance_criteria>
  <done>ort/environment_stress_test.go exists with exactly 3 testing.Short()-gated stress tests (TestStressConcurrentInitDestroy, TestStressRapidInitDestroy, TestStressMixedOperationsUnderLoad) covering concurrent init/destroy, a rapid-cycle variant, and mixed operations under load with fixed iteration counts; all pass under -race locally at -count=5; all report SKIP under -short.</done>
</task>

<task type="auto">
  <name>Task 2: Add the dedicated CI stress job, wire -short into default test invocations, and document stress tests in TESTING.md</name>
  <files>.github/workflows/ci.yml, Makefile, TESTING.md</files>
  <read_first>.github/workflows/ci.yml (lines 1-127, especially the env block at lines 10-14, the test job at lines 68-107, and the test-race-ort-concurrency job at lines 108-126), Makefile (lines 104-120 for the test/test-race targets, lines 347-368 for precommit), TESTING.md (lines 247-301 for the existing Continuous Integration and Local Pre-commit Checks sections)</read_first>
  <action>
    (a) In `.github/workflows/ci.yml`, add a new job named `test-race-ort-stress` as a sibling to (not merged into) `test-race-ort-concurrency`: `runs-on: ubuntu-latest`, `timeout-minutes: 10`, the same checkout (`actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6`) and setup-go (`actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6`, `go-version: ${{ env.GO_VERSION_STABLE }}`, `cache: true`) steps already used elsewhere in this file, a `go mod download` step, and a final step running `go test -v -race -run=TestStress -count=50 -parallel=4 ./ort/...`. Do NOT add these test names to `test-race-ort-concurrency`'s existing `-run` regex (line ~126) — the two jobs must never both run the same tests.

    (b) In the same file's `test` job, add `-short` to both unit-test steps: the Unix step (`go test -v -coverprofile=coverage.out ./...` becomes `go test -v -short -coverprofile=coverage.out ./...`) and the Windows step (`go test -v -coverprofile="${PWD}/coverage.out" ./...` becomes `go test -v -short -coverprofile="${PWD}/coverage.out" ./...`). Leave `test-race-ort-concurrency` and the new `test-race-ort-stress` job's own invocation without `-short` (their curated `-run` regexes already exclude/target the tests they intend to run).

    (c) In `Makefile`, add `-short` to the `test:` target's test command (`$(GO) test -v -cover ./...` becomes `$(GO) test -v -cover -short ./...`) and to `precommit`'s test step (`$(GO) test ./...` becomes `$(GO) test -short ./...`). Leave `test-race` (targets its own curated `-race` subset, never runs `TestStress*`) unchanged.

    (d) In `TESTING.md`, add a new `### Running Stress Tests` subsection immediately after the existing `### Local Pre-commit Checks` section and before `### Local CI Simulation`. Content: explain that `ort/environment_stress_test.go` contains `testing.Short()`-gated stress tests exercising many concurrent `InitializeEnvironment`/`DestroyEnvironment` cycles to guard against refcount corruption, deadlocks, and panics under load; state that they are skipped by default (`go test ./...`, `make test`, and `make precommit` now all pass `-short`); give the local invocation `go test -race -run TestStress -count=10 ./ort/...`; and note that CI runs them in a dedicated `test-race-ort-stress` job with `-count=50` and a 10-minute timeout, separate from `test-race-ort-concurrency`. Also add one bullet to the existing `### GitHub Actions` list (in the `## Continuous Integration` section) describing the new job, matching the existing bullet style (e.g. `- **Stress Test Job**: ...`).
  </action>
  <verify>
    <automated>go test -short -v -run TestStress ./ort/...</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "test-race-ort-stress" .github/workflows/ci.yml returns matches for both the job key and its `name:` field
    - grep -n "run=TestStress" .github/workflows/ci.yml returns exactly 1 match, and it is not inside the test-race-ort-concurrency job (confirm via grep -B8 that the preceding job header is test-race-ort-stress)
    - grep -n "TestValuesToHandlesDeduplicatesRepeatedLockableValue\|TestAdvancedSessionRunAndDestroyConcurrent" .github/workflows/ci.yml still shows the original test-race-ort-concurrency regex unchanged (no TestStress names added to it)
    - grep -n -- "-short" .github/workflows/ci.yml returns matches only in the `test` job's Unix and Windows steps (2 matches)
    - grep -n -- "-short" Makefile returns matches only in the `test:` and `precommit:` targets (not `test-race`)
    - grep -n "Running Stress Tests" TESTING.md returns exactly 1 match
    - go test -short -v -run TestStress ./ort/... shows all 3 TestStress* tests as SKIP (confirms -short wiring actually gates them)
    - go test -race -run TestStress -count=10 ./ort/... -v (the documented local command) passes
  </acceptance_criteria>
  <done>test-race-ort-stress exists as a new, separate CI job (10-minute timeout, -count=50 -parallel=4, no -short); Makefile's test/precommit targets and ci.yml's test job pass -short so TestStress* is skipped everywhere except the dedicated stress job; TESTING.md documents how and why to run stress tests locally, with an updated GitHub Actions bullet list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| CI workflow config | `.github/workflows/ci.yml` changes are maintainer-authored, not attacker-triggered by this phase's work; no new external inputs, secrets, or third-party actions are introduced |
| Stress test internals | `ort/environment_stress_test.go` exercises only in-process refcount/mutex logic against a nonexistent library path — no real FFI, no unsafe pointer traffic, no network or filesystem I/O |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Denial of Service (CI runtime blowup) | .github/workflows/ci.yml (test-race-ort-stress job) | mitigate | `timeout-minutes: 10` guard plus fixed-iteration (not wall-clock-duration) stress tests, sized per RESEARCH.md's measured 0.13s/100x1000 baseline, keep `-count=50` well inside budget |
| T-01-05 | Denial of Service (test hang masking a real deadlock) | ort/environment_stress_test.go | accept | No production lock/lifecycle code changes in this phase; stress tests reuse the existing `mu`/`refCount` invariants already covered by `TestConcurrentInitialization`/`TestConcurrentDestroy` — a genuine deadlock would already be a pre-existing bug, not one introduced here |
</threat_model>

<verification>
1. `go test -race -run TestStress -count=10 ./ort/... -v` passes with zero race warnings (local sanity, matches the documented TESTING.md command).
2. `go test -short -v ./ort/... -run TestStress` shows all 3 stress tests as `SKIP`.
3. `go test -short ./...` (or `make test`) completes without running `ort/environment_stress_test.go`'s tests.
4. `make precommit` (or `go test -short ./...` standalone if `SKIP_LINT_NEW=1 SKIP_GOSEC=1 SKIP_VULNCHECK=1` is set for a fast local pass) completes without running stress tests.
5. `.github/workflows/ci.yml` parses as valid YAML (`yq eval . .github/workflows/ci.yml > /dev/null`) and the new `test-race-ort-stress` job is present alongside, not inside, `test-race-ort-concurrency`.
</verification>

<success_criteria>
- `ort/environment_stress_test.go` exists with 3 `testing.Short()`-gated stress tests covering concurrent init/destroy, a rapid-cycle variant, and mixed operations under load, all passing under `-race`.
- `testing.Short()`'s gate is not cosmetic: `go test ./...`, `make test`, and `make precommit` all skip the stress tests by default; only the dedicated `test-race-ort-stress` CI job (and an explicit local `-run TestStress` invocation) runs them.
- `TESTING.md` documents how to run stress tests locally and what they guard against.
</success_criteria>

<output>
Create `.planning/phases/01-dx-test-hardening/01-03-SUMMARY.md` when done
</output>
