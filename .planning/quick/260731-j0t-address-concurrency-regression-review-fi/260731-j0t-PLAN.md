---
phase: 260731-j0t-address-concurrency-regression-review-fi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - ort/bootstrap.go
  - ort/bootstrap_test.go
  - AGENTS.md
  - .planning/quick/260731-fxc-address-issue-111/260731-fxc-SUMMARY.md
autonomous: true
requirements: [ISSUE-111-REVIEW]
---

<objective>
Turn the reusable-bootstrap-option regression into enforced CI protection, make the
concurrency contract and race-only detection visible, add lane-independent trimming
coverage, and remove unrelated repository-instruction work from this PR.
</objective>

<context>
The production race fix and focused regression already exist on the current PR branch.
The regression only detects the old bug under `-race`, while CI's targeted ORT race
selector currently omits it. Keep whole-package race execution disabled because purego
and checkptr are incompatible; extend only the targeted selector.

Use a source comment rather than build-tag support files to explain the test's race
dependency. Document concurrent reuse only for BootstrapOption values returned by this
package, because callers can define custom stateful option functions.

Remove the branch-only AGENTS.md addition so the current PR returns to its concurrency
scope. Record a complete AGENTS.md pointer and OpenCLIP cache-directory normalization as
separate follow-ups; do not change OpenCLIP behavior here.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce and document the reusable-option concurrency contract</name>
  <files>.github/workflows/ci.yml, ort/bootstrap.go, ort/bootstrap_test.go</files>
  <action>
Append TestBootstrapOptionsReusableConcurrently to the fully anchored RACE_SELECTOR and
bump both expected counts from 33 to 34. Extend BootstrapOption godoc to state that option
values returned by the package may be reused concurrently. Add a one-line comment above
the regression explaining that only the race detector catches shared captured-state
writes. Rename iterationsPerWork to iterationsPerWorker and use `for range` for the
iteration loop. Add non-race assertions that whitespace is trimmed from version, library
path, and cache-directory values while retaining the existing empty-value checks.
  </action>
  <verify>
Run the selector's list/count guard and its exact `go test -race` command. Run the focused
constructor tests without `-race` to prove trimming independently of the detector.
  </verify>
  <done>
CI selects exactly 34 ORT race tests including the regression, the public contract is
documented, and ordinary tests directly assert all requested trimming behavior.
  </done>
</task>

<task type="auto">
  <name>Task 2: Restore PR scope and record follow-ups</name>
  <files>AGENTS.md, .planning/quick/260731-fxc-address-issue-111/260731-fxc-SUMMARY.md, .planning/quick/260731-j0t-address-concurrency-regression-review-fi/260731-j0t-deferred-items.md</files>
  <action>
Delete the AGENTS.md file added only on this branch, preserving its work as a recoverable
git change. Correct the prior quick-task summary so it no longer reports that the overall
branch had no plan-scope deviation. Record two deferred items: land a concise AGENTS.md
pointer to CLAUDE.md in a separate PR, and open a separate OpenCLIP issue for storing the
trimmed cache path.
  </action>
  <verify>
Confirm the diff against origin/main contains no AGENTS.md and no OpenCLIP source changes,
and that the deferred-items file names both follow-ups.
  </verify>
  <done>
The concurrency PR contains no unrelated AGENTS.md addition, the earlier summary is
accurate, and both out-of-scope findings remain visible for separate work.
  </done>
</task>

</tasks>

<verification>
Run gofmt on the edited Go files, the exact CI race selector/count command, focused
non-race constructor tests, and the repository precommit gate after the final code edit.
Inspect the final diff and status before every commit.
</verification>

<success_criteria>
- Reverting the production fix would make an automated CI lane fail under the race detector.
- Package-provided BootstrapOption values are documented as concurrently reusable.
- Version, library-path, and cache-directory trimming are asserted without `-race`.
- The regression source states why `-race` is required and uses consistent loop naming/syntax.
- AGENTS.md and the OpenCLIP behavior change are absent from this PR's source diff.
- Quick-task plan, summary, deferred items, and STATE.md are committed separately from code.
</success_criteria>
