# Repository Instructions

- Always work on a dedicated pull-request branch. Before making changes, create or switch to a descriptive non-`main` branch; never commit directly to `main`.
- Use squash merge when integrating a pull request.
